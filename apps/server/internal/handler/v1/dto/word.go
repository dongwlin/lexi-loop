// Package dto 定义 /api/v1 版本化请求 / 响应 DTO 与转换函数，
// 序列化形状只由本包定义（JSON 字段统一 camelCase）。
package dto

// TODO: Word 请求 / 响应 DTO（docs/api/words.md）：
// 如 ImportWordsRequest、WordItem、ListWordsResponse 及 toXxx 转换函数。
