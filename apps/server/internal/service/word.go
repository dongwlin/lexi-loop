// Package service 提供业务用例编排：具体 Service 类型，构造返回具体指针；
// 请求/结果类型放本包且不含 json 标签。JSON 契约由版本化 DTO 定义。
package service

// TODO: WordService —— ImportWords / ListWords / GetWord /
// UpdateReviewMeaning / DeleteWord（docs/api/words.md）。
