# 2026-09 Agent Log

> 历史流水：每条记录描述其时间点的状态；旧记录中的“当前”“下一步”“未实现”不代表现状。阶段状态见 [Roadmap](../product/roadmap.md)，设计规则以现行领域文档为准。

## 2026-09-08 12:35:29 +08:00 — 增加 GitHub CI 与 GHCR 容器发布

- 完成：新增可复用 GitHub Actions CI，覆盖 Go 格式 / vet / build / race 与真实 PostgreSQL 集成测试、前端类型 / lint / Vitest / Storybook / Playwright 生产构建冒烟，以及 OpenAPI / API client 生成一致性；集成环境不可用导致跳过时 CI 失败。稳定版本 tag 触发检查后构建含校验词典数据的 amd64 镜像，使用 GITHUB_TOKEN 发布到 GHCR，输出版本 tag / latest 与 digest。Compose 支持镜像仓库与版本覆盖，本地发布脚本默认名同步，更新发布权威文档与导航。
- 涉及：`.github/workflows/ci.yml`、`.github/workflows/release.yml`、`docker-compose.yml`、`deploy/release.sh`、`Dockerfile`、`docs/deploy/release.md`、`docs/README.md`。
- 验证：actionlint 通过；Go vet / build / race（365 个测试含子用例，无跳过）、OpenAPI 与 API client 重生成 diff 检查通过；pnpm typecheck / lint / 全量 Vitest、Storybook build、Playwright 2 个生产构建冒烟通过；词典 SHA256 校验、Compose 默认与覆盖镜像解析、bash -n 与 git diff --check 通过。浏览器测试首次受沙箱限制失败，允许本地浏览器运行后通过。尚未推送工作流或 tag，未实际向 GHCR 发布镜像。

## 2026-09-08 07:52:59 +08:00 — 修复前端类型检查空跑与复习集成测试高负载超时

- 完成：web `typecheck` 改为 `vue-tsc -b`，遍历根 tsconfig 引用的 app / node 子项目；同步前端技术栈与 web 指引。复习页与结果页集成测试在 `beforeAll` 中预加载本套件访问的页面模块，将 Vite 首次转换与模块加载移出业务用例的 5s 时限；保留真实 RouterView / MSW 请求链路，不增加超时或重试，不改业务代码。测试规范补充懒加载页面的准备约定，复习页测试顺带按现有 Prettier 配置格式化。
- 定位：正常全量 213 用例通过；限制到 4 个 CPU、显式 16 workers 后，旧测试两轮均复现复习首测 5s 超时。临时计时确认首次 `router.push` 等待模块加载分别约 5092 / 5141ms，超时后的未完成导航还会干扰后续用例；计时诊断代码已移除。
- 涉及：`apps/web/package.json`、`apps/web/AGENTS.md`、`apps/web/tests/integration/review-page.test.ts`、`review-result-page.test.ts`、`docs/specs/frontend/前端技术栈.md`、`前端测试规范.md`。
- 验证：临时 Vue / tests / e2e 文件各注入一个类型错误，旧命令退出 0，新 `typecheck` 退出 1 并定位全部三处错误（探针已删除）；根 `pnpm typecheck`、web `lint` / `build` / `storybook:build` 通过。相同受限 CPU / 16 workers 下全量连续三轮各 33 文件 213 用例通过（后两轮乱序 seed 42 / 2026）；首次复习业务用例降至约 1s，未调整默认时限与 retry。`git diff --check` 通过。

## 2026-09-08 07:34:06 +08:00 — 修复 PR #5 词典导入状态衔接的审查问题

- 完成：自动检查开启时构造即为 checking，覆盖 HTTP 可用但后台尚未调度或文件检查尚未完成的窗口；确认缺少 CSV 或 manifest 不可用后才收敛为 idle，关闭开关仍初始 idle。前端仅在 checking / importing 转为 completed 时弹就绪通知，首次读取 completed 不弹。同步 Meta API 状态机说明，补充启动窗口 API 测试、首次响应即时断言与两条完成迁移测试；文件检查测试不再依赖数据库，使用状态等待代替固定休眠。
- 涉及：`apps/server/internal/service/dictimport.go`、`dictimport_test.go`、`apps/server/internal/handler/dictimport_test.go`、`apps/web/src/features/meta/components/DictImportIndicator.vue`、`DictImportIndicator.test.ts`、`docs/api/meta.md`、`docs/agent-log/2026-09.md`
- 验证：`go test -race -count=1 ./internal/service ./internal/handler` 通过（首次受 Docker 访问限制跳过集成，随后获准访问本机 Docker 并补跑 PostgreSQL 集成测试通过；Go 构建缓存放在 `/tmp`）；前端定向 Vitest 9 用例、`vue-tsc -b`、相关文件 ESLint 通过；`git diff --check` 通过。

## 2026-09-08 01:30:00 +08:00 — 词典随镜像自动导入（serve 后异步）+ 前端右上角导入进度指示

- 完成：实现 issue #3（方案 A · 异步导入版，右上角指示采用评审定稿的变体 A 紧凑徽标）。**server**：`internal/importer/ecdict` 的 `Import` 增加 `sourceVersion` 参数并写入词条 `source_version`（`sourceName` 导出为 `ecdict.SourceName`）；`repo.CountBySource(source, version)` 支撑版本完整性守卫；config 新增 `LEXI_DICT_CSV`（缺省 `/app/data/ecdict.csv`）与 `LEXI_DICT_AUTOCHECK`（缺省 true）；`service.DictImportService` 编排 serve 启动后的守卫式异步导入（进程内 mutex 状态机 idle/checking/importing/completed/failed，单飞、失败不退出、优雅关闭在批次边界停止），Meta API 新端点 `GET /api/v1/dictionary-import`（恒成功，huma 生成 spec 同步 `docs/openapi/`）；`import-ecdict` 保留并补 `--source-version`；组合根在 HTTP 启动后拉起导入 goroutine。**deploy**：`deploy/dict/fetch.sh`（pinned ECDICT commit `82c9872` + sha256 `1a6947e0…` + 幂等 manifest 生成）、`Dockerfile` 零网络 `COPY deploy/dict/ → /app/data/`（缺文件警告产出无词典镜像）、`release.sh --strict` 严格模式、根 `.gitignore` 忽略数据产物。**web**：api-client 重生成（`getDictionaryImport` 补入显式导出清单）、`features/meta` 新增 `dictImportRefetchIntervalMs`（checking/importing 3s、failed 15s、completed/idle 停止轮询）与 `DictImportIndicator` 变体 A 徽标（点击 popover 详情含 progressbar 语义与续传指引，completed 一次性「词典已就绪」toast）；`components/ui/popover/Popover.vue` 新组件（Reka 配方 + Story）；AppShell 经 `header-actions` 插槽承载（app/App.vue 填充，布局组件不读业务 Query）。**文档**：`api/meta.md §3` 权威契约、`deploy/release.md` 数据依赖与严格模式、`backend/structure.md`（service → importer 组装边等）、`docs/README.md` 导航与初始化步骤、`architecture/overview.md`、`dictionary/overview.md` / `enrichment.md`、`roadmap.md`、`apps/server/README.md`、`apps/web/AGENTS.md`。
- 涉及：`apps/server/**`（importer / repo / config / service / handler / app / cmd / 测试）、`docs/openapi/*`、`Dockerfile`、`deploy/dict/fetch.sh`、`deploy/release.sh`、`.gitignore`、`packages/api-client/**`、`apps/web/**`（features/meta、components/ui/popover、AppShell、App）、`docs/**`（api / deploy / dictionary / architecture / backend / product / README）、`docs/agent-log/2026-09.md`
- 验证：server `go test -race -count=1 ./...` 14 包全绿（含 DictImport 守卫 / 导入 / 失败 / 取消 / 单飞集成测试与 OpenAPI 11 操作断言）；web `typecheck`、`lint`、`build` 通过，DictImportIndicator 组件测试 8 用例绿（fake timers 验证轮询节奏与 toast 收敛），storybook project 16 用例绿（含 2 个新 Popover Story + axe 门禁）；真实数据 E2E：临时 PG + 真实 CSV（770,611 行）`serve` 全新库 → checking→importing（进度实时推进）→ completed（约 51s），导入期间 `POST /api/v1/words/import` 正常响应，重启后 `dict already complete, skip import` 秒过，无 CSV 的 `go run . serve` 保持 idle 无报错。
- 备注：web 全量 `test:run` 中 review-page / review-result 3 个用例在本机负载下偶发 5s 超时，已用 git worktree 在 main 上复现同样失败，属既有抖动而非本改动引入。评审原型 `prototypes/` 目录为未入库一次性产物，评审完成后可删。

## 2026-09-07 17:50:00 +08:00 — 发布 v0.0.2 并部署到生产 podman 栈

- 完成：发布 v0.0.2（自 v0.0.1 起含 PR #2 复习数量选择交互与文档重组）。流程：三处 package.json 版本 bump（根 / `apps/web` / `packages/api-client`）→ annotated tag `v0.0.2` → `deploy/release.sh -r podman` 构建镜像 → 生产部署。首次构建被 web 构建步骤阻断：`vue-tsc -b` 对集成测试报 TS2345——`review-page.test.ts` 三处 `JSON.parse(localStorage.getItem(...))` 把 `string | null` 传入 `JSON.parse(string)`；PR #2 只跑过 typecheck / lint / vitest 未跑 build，而 `typecheck` 脚本（`vue-tsc --noEmit` 打在 `files: []` 的 solution 风格根 tsconfig 上）实际不检查任何文件，类型错误因此漏网。按 `review-snapshot.test.ts` 既有惯例加非空断言修复（断言前测试刚写入该 key，运行时行为不变），tag 重打到修复提交后重建成功（`localhost/lexi-loop:v0.0.2` = `:latest`，86.5MB）。部署：生产栈为用户级 systemd 单元托管的 compose（`podman-compose up` / `down`，lexi-loop 用 `lexi-loop:latest`），升级按既定约定全量重建容器，迁移随入口脚本执行。
- 涉及：`package.json`、`apps/web/package.json`、`packages/api-client/package.json`、`apps/web/tests/integration/review-page.test.ts`、git tag `v0.0.2`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web build`（vue-tsc -b + vite build）通过；全量 `test:run` 31 文件 202 用例绿；`podman run --rm --entrypoint /app/lexi-loop localhost/lexi-loop:v0.0.2 version -b` 输出 v0.0.2 + 构建时间 + go1.26.8；部署后容器 healthy，`curl http://127.0.0.1:9080/api/v1/version` 返回 v0.0.2（buildTime / goVersion 一致），`/healthz` OK，首页 index.html 正常。首次构建尝试另遇 corepack / pnpm 拉取 npm registry 瞬时网络失败（ECONNRESET），重试自愈。
- 备注：① `typecheck` 脚本为无操作（1 秒零诊断），建议后续任务改为 `-b` 或 `-p tsconfig.app.json`（本次不动，避免发版夹带额外变更）。② buildah 悬空中间镜像按 release.md 约定保留作增量构建缓存，未清理。③ 本条日志提交位于 tag 之后（`.dockerignore` 排除 docs，镜像内容与 tag 树一致）。

## 2026-09-07 17:40:00 +08:00 — 「自定义」按钮与输入框钉在同一 w-20 槽位

- 完成：新 review 意见（P2）指出「自定义」按钮为内容自适应宽度（px-4 + 三字 ≈ 74px）而输入框固定 w-20（80px），两者不同宽，flex-wrap 临界视口宽度下替换可能引发跳动甚至换行，且此前「预设按钮零位移」的验证无法证明该槽位自身几何不变。修复：按钮加 `class="w-20"`，与输入框共用同一固定槽位——替换前后占位逐字段一致，换行条件与替换动作解耦。集成测试在替换前后各断言一次 `toHaveClass('w-20')` 钉住不变量；`review-flow.md` §6 宽度描述由「接近」改为「同一固定宽度槽位（换行条件不因替换而变化）」。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/tests/integration/review-page.test.ts`、`docs/frontend/review-flow.md`、`docs/agent-log/2026-09.md`
- 验证：Prettier / `lint` 通过，review-page 集成测试 14 用例全绿；真实 Chromium 实测：默认视口下按钮（BUTTON）与替换后输入框（INPUT）占位逐字段相同（80×44 @ x571.84, y185）；视口宽度扫描 400→320px：400px 时替换前后均在第一行（y185），380–320px 时替换前后均已换行且位置精确相同（y237）——任何宽度下替换都不引发新的布局变化。

## 2026-09-07 17:30:00 +08:00 — 自定义数量 placeholder 去掉「例如」前缀

- 完成：用户要求自定义数量输入框 placeholder 不带「例如」，由「例如 25」改为「25」。生词库搜索框的同款前缀不在本 PR 范围，未动。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`
- 验证：Prettier / `lint` 通过；review-page 集成测试 14 用例全绿。

## 2026-09-07 17:20:00 +08:00 — 复核收尾：钉住截断提示门控并修正清错措辞

- 完成：终审复核（结论可合并，无 P0/P1/P2 遗留）指出 3 条不阻塞 P3，处理其二：新增集成测试钉住截断提示门控（可复习量 3 时选 50 出现 D009 提示 → 切自定义空输入隐藏 → 输入有效 4 恢复）；`review-flow.md` §6 清错措辞按实现放宽为「再次输入即清除，开始时仍无效会重新提示」。第三条（`id="custom-count"` 回退后无引用）按复核意见保留作稳定锚点，不处理。
- 涉及：`apps/web/tests/integration/review-page.test.ts`、`docs/frontend/review-flow.md`、`docs/agent-log/2026-09.md`
- 验证：Prettier / `lint` 通过；review-page 集成测试 14 用例全绿。

## 2026-09-07 17:10:00 +08:00 — 按用户裁决回退自定义输入框的可见标注

- 完成：用户裁决数量选项行是选择器而非表单场景，交互与可访问性规范 §8.1 的「持久可见 Label」不在遵循范围内，且可见「自定义」标注破坏 issue #1「仅该选项原地替换」的 UX。自定义输入框回退为 `aria-label="自定义数量"`（宽度恢复 `w-20` 对齐选项），删除 wrap label 与可见标注；保留 `inputmode="numeric"`（不可见、移动端收益）与 §8.4 的 `aria-invalid` / `aria-describedby` / 稳定 ID 错误关联。范围裁决理由登记在模板注释与 `review-flow.md` §6（含日期），供后续审查免重复争议。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/tests/integration/review-page.test.ts`（可达名回退「自定义数量」）、`docs/frontend/review-flow.md`、`docs/agent-log/2026-09.md`
- 验证：`lint` / Prettier 通过；review-page 集成测试 13 用例与 review-count-preference 单测全绿；真实 Chromium 确认输入框原地替换、预设按钮零位移、错误态 aria 关联不回退。

## 2026-09-07 16:50:00 +08:00 — 按 PR #2 审查意见修复数量选择交互（全部发现项）

- 完成：逐项落实子 Agent 对 PR #2 的审查发现。P1-1 自定义输入框改为「自定义」可见标注 + 输入框的组合（wrap label，满足交互与可访问性规范 §8.1 持久可见 Label，placeholder 退回格式示例），补 `name="review-count"` 与 `inputmode="numeric"`；P1-2 错误提示加稳定 ID `custom-count-error`，输入框按错误状态绑定 `aria-invalid` / `aria-describedby`（§8.4）。P2-1 `enterCustomMode` 在带回有效残留输入时同步 `saveReviewCountPreference`，消除「屏幕选中态与存储分叉」；P2-2 「十进制安全正整数」规则下沉为 `parsePositiveCount`（util 内 `isPositiveCount` 与加载校验共用），页面三处调用点收敛。P3-1 双 rAF 焦点校正抽取为 `refocusAfterTransition(focus, isFocusOnTarget)`，复习区域与自定义输入框共用；P3-3 截断提示加 `hasUsableSelection` 门控（自定义输入无效时不再与「请输入正整数」并存）；P3-4 单测补 5 个非法回退用例（顶层 null/数字、负数、version/count 字符串）+ `parsePositiveCount` 11 个用例；P3-5 集成测试钉死三个行为（自定义→预设→再自定义的残留输入与存储一致性、再次输入清错并取消无效标记、逐键持久化回退停留最后有效值）；P3-2 悬挂 id 随 P1-2 复用消解；P3-6 联合类型按审查意见保留。`review-flow.md` §6 同步（双状态结构图、标注保留说明、错误关联与清错、截断提示门控、逐键持久化与切换一致性语义）。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/src/utils/review-count-preference.ts` 及同名单元测试、`apps/web/tests/integration/review-page.test.ts`（可达名改为「自定义」，新增 3 个用例）、`docs/frontend/review-flow.md`、`docs/agent-log/2026-09.md`
- 验证：`typecheck` / `lint` 通过；改动文件 Prettier；全量 `test:run` 31 文件 201 用例全绿；真实 Chromium 实测：预设按钮坐标切换前后逐一相同（零位移）、标注 + 输入框同位替换并聚焦、错误态 `aria-invalid="true"` + `aria-describedby="custom-count-error"`、custom 25 → preset 20 → 再自定义后存储同步回 `{custom,25}`。

## 2026-09-07 15:05:00 +08:00 — 优化复习数量选择交互并记忆上次选择（issue #1）

- 完成：复习页数量选择改为同一组选项 `10 / 20 / 30 / 50 / 自定义`（自定义紧跟 50），删除独立的「自定义数量」输入区与「确认」按钮；点击「自定义」后仅该选项原地替换为数字输入框（宽度与选项接近、文字居中，进入即聚焦，无布局位移），输入有效数量即时作为选中值，点「开始复习」直接使用，无效输入（空 / 非正整数）就地提示「请输入正整数」并阻止开始。新增 `utils/review-count-preference`：数量选择（预设值，或自定义模式及其输入值）按版本化格式持久化到 localStorage（`lexi-loop.review-count`，沿用 theme / review-snapshot 的 adapter 与降级策略），setup 时恢复上次选择，无记录或非法数据回退默认 30；预设列表 `PRESET_COUNTS` 与默认值随 util 一并收敛。同步权威文档 `review-flow.md` §6（页面结构图、交互描述与持久化规则，标注 2026-09-07 定稿，顺带移除图中早已不存在的「建议复习」行）。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/src/utils/review-count-preference.ts`（新增）及同名单元测试、`apps/web/tests/integration/review-page.test.ts`（新增 6 个数量选择用例）、`docs/frontend/review-flow.md`、`apps/web/AGENTS.md`（测试覆盖清单）、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` 与 `lint` 通过；`vitest` web project 下 review-count-preference 单元测试 13 例、review-page 集成测试 10 例（4 恢复 + 6 数量选择）全部通过；改动文件 Prettier 格式化；分支 `feat/review-count-selection`。

## 2026-09-07 13:52:11 +08:00 — 整理 MVP 完成后的现行文档与历史归档

- 完成：Roadmap 标明 MVP 已完成及 v0.0.1 发布基线，旧工程落地顺序迁入 archive 并标明失效范围；文档中心与根指引区分现行规则、后续规划、工程规范和历史流水。应用 README / AGENTS 收拢重复完成清单，补齐页面路由、明确认证刷新与在线词典增强尚未实现，统一后续规划入口。修复缺失规范引用，将现存规范的 Wiki 引用转换为相对 Markdown 链接，并修正相关章节编号；保留现行 API、模型、公式与冻结决策。
- 涉及：`docs/README.md`、`docs/product/`、`docs/archive/`、`docs/frontend/review-flow.md`、`docs/dictionary/`、`docs/specs/`、根与应用 `AGENTS.md`、应用及 API Client `README.md`、`docs/agent-log/2026-09.md`
- 验证：核对当前路由、API Client 认证注入、OpenAPI 业务 422 配置与 v0.0.1 标签；检查现行文档及归档的 381 个本地 Markdown 链接，目标均存在；历史计划正文与迁出前内容一致；`git diff --check` 通过。仅修改 Markdown，未运行应用测试。

## 2026-09-07 13:30:00 +08:00 — podman 侧补齐 v0.0.1 发布镜像并沉淀运维事实

- 完成：用户指出 podman 侧发布镜像缺失——确认 docker / podman 镜像存储相互独立，此前正式构建只进了 docker 存储，podman 里的 `localhost/lexi-loop:latest` 还是被 `latest` 抢占标签的 rc 测试镜像（内含 v0.0.1-rc1 版本号）。按文档流程 `git checkout v0.0.1` 后以 `-r podman` 重建发布镜像，podman 现有 `localhost/lexi-loop:v0.0.1` 与 `:latest`（同镜像 ID，86.5MB），`version -b` 验证注入正确。清理本次 podman 实测产生的 6 个悬空镜像（5 个旧中间 stage + 1 个被取代的 rc 成品，约 2.3GB），不动用户既有悬空镜像与本次构建留下的活跃 stage 缓存。`docs/deploy/release.md` 补两条运维事实：存储独立（两边都要镜像就各跑一次脚本）、buildah 悬空镜像即增量构建缓存（清理会让下次构建回冷路径，`podman image prune` 会连同其它悬空镜像一起删）。
- 涉及：`docs/deploy/release.md`、`docs/agent-log/2026-09.md`
- 验证：detached 于 v0.0.1 tag、工作区干净状态下 `deploy/release.sh -r podman` 成功（web stage 与 go mod 全部 `Using cache`，go build 因 ldflags 变更重跑，数分钟）；`podman run --rm --entrypoint /app/lexi-loop localhost/lexi-loop:v0.0.1 version -b` 输出 `version: v0.0.1` + 构建时间 + go1.26.8；`podman images` 确认双 tag 指向同一镜像 ID；悬空镜像清理后仅余 1 个活跃构建缓存与用户既有 3 个。
- 备注：HEAD 已回到 main（tag 之后的提交不参与镜像构建，docs 已被 `.dockerignore` 排除）。

## 2026-09-07 12:27:00 +08:00 — 建立发布流程（tag → release.sh → compose）并发布 v0.0.1

- 完成：确定发布流程「打 annotated tag → `deploy/release.sh` 构建镜像 → `docker compose up`」，新增权威文档 `docs/deploy/release.md` 并注册进 `docs/README.md`（目录树 / 权威表 / 快速定位）与根 `AGENTS.md` 权威表。`deploy/release.sh`（新增）：`-r/--runtime docker|podman` 切换运行时（缺省自动探测 docker→podman，支持 `CONTAINER_RUNTIME` 环境变量）、`-i/--image` 镜像名（可含 registry 前缀，缺省 `lexi-loop`，`LEXI_IMAGE`）、`--push`；构建前强校验「工作区干净 + 版本 tag 存在且指向当前 HEAD」（防止拿未发布内容打出正确版本号），以 `--build-arg VERSION=<tag>` 注入版本并同打 `:版本` 与 `:latest`（latest 供 compose 直接使用）。两处 podman 兼容修复（均实测踩到）：Dockerfile / docker-compose.yml 基础镜像改全限定名 `docker.io/library/...`（podman 默认强制短名称交互确认，无 TTY 直接失败，首次 podman 构建在 caddy 阶段失败）；脚本对 podman 固定 `--format docker`（podman 默认 OCI 格式不支持 HEALTHCHECK、静默丢弃）。版本号 bump 0.0.1：根 / `apps/web` / `packages/api-client` 的 package.json（运行时版本仍以 tag → buildinfo 为单一来源）；Dockerfile VERSION 注释改指向脚本。
- 涉及：`deploy/release.sh`（新增）、`docs/deploy/release.md`（新增）、`docs/README.md`、`AGENTS.md`、`Dockerfile`、`docker-compose.yml`、`package.json`、`apps/web/package.json`、`packages/api-client/package.json`、`docs/agent-log/2026-09.md`
- 验证：失败路径全部按预期非零退出（--help 正常、未知选项、HEAD 无 tag、显式传入不存在的 tag、tag 存在但不指向 HEAD、脏工作区）；临时 tag 实测 docker 构建 + compose 冒烟（`/api/v1/version` 返回注入版本，栈 `down -v` 清理）；podman 全量构建成功且 `podman run --entrypoint /app/lexi-loop … version -b` 验证注入正确（v0.0.1-rc1 + 构建时间 + go1.26.8），HEALTHCHECK 保留。打 `v0.0.1` tag 后完成发布构建：`lexi-loop:v0.0.1` / `:latest`（121MB），`version -b` 输出 `version: v0.0.1`。临时 tag 与 rc 镜像均已清理。
- 备注：本条日志提交位于 v0.0.1 tag 之后（日志不参与镜像构建，`.dockerignore` 已排除 docs），镜像内容与 tag 树一致。

## 2026-09-07 11:13:00 +08:00 — 新增版本信息：API 端点 / 「关于」页 / CLI version 子命令

- 完成：版本信息单一来源 `internal/infra/buildinfo`（`Version` / `BuildTime` 由 go build ldflags `-X` 注入，开发构建版本以 `dev` 占位、构建时间空表示未知；`GoVersion()` 取 `runtime.Version()`）。① 后端新端点 `GET /api/v1/version`（`handler/v1/version.go` + `dto/version.go`，`VersionHandler` 无 Service 依赖；无入参恒成功、不声明 Errors，spec 归一化顺带移除 huma 对未声明 Errors 操作自动追加的 default 残留响应），注册进 `RegisterRoutes` / 组合根 / 离线 spec 构建；openapi 单测操作数 9 → 10 并对 get-version 豁免 400 断言，新增不依赖数据库的版本端点组件测试。② CLI 新增 `lexi-loop version` 子命令：默认只输出版本号，`-b` 输出 `version` / `build_time` / `go_version` 三行（buildTime 空显示 `-`），带 cmd 单测。③ 前端新增「关于」页 `/about`（主导航加「关于」入口）：展示应用版本 / 构建时间 / Go 版本，构建时间复用 `formatDateTime`（空值展示「—」，未新增格式化工具）；feature 层 `features/meta/api`（keys + `useVersionQuery`，queryFn 透传 signal）+ keys 单测；MSW 工厂 `makeVersionInfo` + 集成测试（三字段渲染、dev 与空 buildTime 占位、错误分支）；重生成 openapi spec 与 api-client（新增 `getVersion`，index.ts 补导出）。④ 构建注入：Dockerfile `ARG VERSION=dev` + ldflags 注入 Version / BuildTime（取镜像构建时刻），docker-compose 透传宿主环境 `VERSION`。⑤ 文档：新增 `docs/api/meta.md`（Meta API 权威契约：响应示例、dev 占位规则、buildTime 空值语义、CLI 对照）；`docs/README.md`、根 `AGENTS.md`（权威表与文档地图）、`docs/backend/structure.md`（目录树 / CLI 清单 / 契约引用）、`docs/architecture/overview.md`、`apps/server/README.md` 与 `AGENTS.md`、`apps/web/README.md` 与 `AGENTS.md` 状态段同步。
- 涉及：`apps/server/`（`internal/infra/buildinfo/` 新增、`cmd/version.go` 与 `cmd/root.go`、`handler/v1/version.go`、`handler/v1/dto/version.go`、`handler/router.go`、`handler/openapi.go`、`handler/openapi_test.go`、`handler/version_test.go`、`handler/main_test.go`、`internal/app/server.go`、`README.md`、`AGENTS.md`）、`Dockerfile`、`docker-compose.yml`、`docs/openapi/`（重生成）、`packages/api-client/`（重生成 + `index.ts`）、`apps/web/`（`features/meta/api/`、`pages/about/AboutPage.vue`、`app/router.ts`、`components/layout/AppShell.vue`、`tests/mocks/handlers.ts`、`tests/integration/about-page.test.ts`、`README.md`、`AGENTS.md`）、`docs/api/meta.md`（新增）、`docs/README.md`、根 `AGENTS.md`、`docs/backend/structure.md`、`docs/architecture/overview.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...` 与 `go test ./...` 全绿（含 testcontainers 集成测试）；`pnpm -F @lexi-loop/web test` 164 用例全绿（期间一次全量并行下既有 review-result 用例超时，单独重跑 1s 通过，属负载偶发，复跑全量通过）；`pnpm -F @lexi-loop/web build`（vue-tsc + vite）与新增文件 ESLint 通过；ldflags 注入冒烟：注入构建后 `version` / `version -b` 输出正确，临时 Postgres + `migrate up` + `serve` 后 `curl /api/v1/version` 返回注入值（`v0.1.0-smoke` + 构建时间 + Go 版本），未注入二进制返回 `dev` 占位；验证后临时容器与进程已清理。

## 2026-09-07 10:15:27 +08:00 — 新增 Docker 单镜像部署（web + server，Caddy 反代）

- 完成：按用户要求编写 Docker 化部署，单镜像同时包含 web 与 server：Caddy 托管 web 构建产物并反代 `/api`、`/healthz` 到同容器 Go server（同源部署，无需 CORS）。多阶段 Dockerfile——`node:24-alpine` + corepack pnpm 11.13.1 构建 web（`VITE_API_BASE_URL` 留空走同源相对路径）、`golang:1.26-alpine` 以 `CGO_ENABLED=0` 静态编译 server（迁移 SQL 已 embed，无需外挂文件）；最终 stage `caddy:2-alpine`，`deploy/Caddyfile` 实现 SPA History 路由回退与 `/assets/*` immutable 缓存头；`deploy/docker-entrypoint.sh` 作为 PID 1 先显式执行 `lexi-loop migrate up`（符合「启动不隐式迁移」约定，调用的是 migrate 命令）再并行启动 server 与 Caddy，trap 转发 SIGTERM/SIGINT 保留 server 优雅关闭。附 `docker-compose.yml`（app + postgres:18-alpine、`service_healthy` 依赖、pgdata 具名卷、宿主端口 `${LEXI_HTTP_PORT:-8080}`，连接串指向 db 服务）与 `.dockerignore`（排除 node_modules / dist / docs / .env / 测试产物，保留 migrations）。
- 涉及：`Dockerfile`、`deploy/Caddyfile`、`deploy/docker-entrypoint.sh`、`docker-compose.yml`、`.dockerignore`（均新增）、`docs/agent-log/2026-09.md`
- 验证：`docker build` 两阶段构建成功（镜像 121MB）；`docker compose up` 后：`GET /healthz` 返回 `{"status":"ok"}`，`/` 与深链 `/review` 均返回 index.html，`/assets/*.js` 带 `max-age=31536000, immutable`，`/api/v1/words` 经反代返回标准 envelope，未知 `/api` 路径 404 为 gin 文本（确认到达 Go）；app 日志确认 `migrate up` 成功执行，HEALTHCHECK 状态 healthy，`docker compose stop` 1.5s 内 Caddy 与 server 均优雅退出；验证后栈与卷已清理。

## 2026-09-07 09:40:00 +08:00 — 新增放弃复习端点并修复恢复后键盘焦点失效

- 完成：按用户要求修复两项。① abandon 端点（先改权威文档）：`docs/api/reviews.md` §1 总览新增 `POST /api/v1/reviews/{sessionId}/abandon`、§3 补充「session 已非 active 时提交幂等短路」、新增 §6 契约——active → abandoned、对 abandoned 幂等返回成功、completed 返回 422（复用标准业务前置 code `BASE.BIZ.USER_DISABLED`，message 为 `review session is not active`）、不存在 404；`docs/review/data-model.md` §4 与 `docs/frontend/review-flow.md` §6 同步放弃行为（前端调用端点，网络 / 5xx 保留快照可重试，404 / 422 视为轮次已结束）。服务端：`ReviewService.AbandonSession` 持 session 行锁（与提交同锁顺序）经 Domain `Abandon` 迁移；`submitResultTx` 锁定 session 后新增非 active 幂等短路，防止放弃 / 完成后迟到提交计入已结束轮次；handler 注册操作、openapi `keepBusiness422` 登记 abandon 的 422 语义；新增 service 集成测试（幂等放弃、completed 拒绝、不存在 404、放弃后提交不计数）与 HTTP 契约测试，openapi spec 单测更新为 9 操作，重生成 `docs/openapi/`。api-client 重生成并补 endpoints 单测。前端：`useAbandonReviewSessionMutation`（失效 session 汇总查询）、`describeReviewAbandonError` 文案映射（与开始失败共用瞬时错误分支）、ReviewPage「放弃本轮」接入端点（pending 态、恢复入口保留可重试、404 / 422 清快照回配置视图）并更新 snapshot 模块头注释。② 恢复后键盘焦点失效（用户人工复现确认）：根因为浏览器对「移除已聚焦按钮」的焦点归还（focus fixup）作为任务排在 Vue 渲染微任务之后，仅 `nextTick` 聚焦被覆盖回 body；修复为 `focusReviewAreaAfterTransition`——nextTick 聚焦后跨两帧检查焦点是否仍在复习区域、不在则再聚焦，统一应用于开始 / 恢复 / 揭示 / 作答四个迁移点（handleReveal 随之去掉多余的 async）。③ 用户列出的第 3 项（In-app Browser 合成指针首次点击被吞）为测试工具链环境限制，仓库内无修改对象，维持既有日志记录。状态同步：`apps/web/README.md`（端点数 9、复习页条目改为 abandon 行为）、`apps/web/AGENTS.md`（状态段与 D010 条目）、`apps/server/AGENTS.md`（端点数与 abandon 登记）。
- 涉及：`docs/api/reviews.md`、`docs/review/data-model.md`、`docs/frontend/review-flow.md`、`docs/openapi/`（重生成）、`apps/server/`（service/review.go、service/review_test.go、handler/v1/review.go、handler/openapi.go、handler/openapi_test.go、handler/router_integration_test.go、AGENTS.md）、`packages/api-client/`（重生成 + endpoints.test.ts）、`apps/web/`（features/review/api/mutations.ts、utils/review-errors.ts 及测试、pages/review/ReviewPage.vue、utils/review-snapshot.ts 注释、tests/integration/review-page.test.ts 新增、README.md、AGENTS.md）、`docs/agent-log/2026-09.md`
- 验证：`apps/server` gofmt / `go build` / `go vet` 无报错，`go test ./...` 全绿（新增 abandon 集成与契约用例）；`pnpm -F @lexi-loop/api-client generate` 后 `test:run` 25 用例通过；`pnpm -F @lexi-loop/web lint` / `format` / `build` 无报错、`test:run` 28 文件 159 用例全绿（新增 review-page 集成 4 用例：恢复后焦点、放弃成功 / 5xx / 404）。真实浏览器（Vite dev + 本地后端 + 测试库）验证两条修复：用户中断的 7/9 轮次点「继续复习」后零点击直接按 Space 立即揭示释义、焦点在复习区域（tabindex=-1 DIV）；答完该轮跳结果页统计正确；新一轮答 1 题刷新后点「放弃本轮」→ `POST …/abandon` 200（server 日志 request_id 可查）、DB session 置 `abandoned`、UI 回数量配置视图。按需提交 5 个：fc8d05d（权威文档）、abc0615（服务端 + spec）、9786e25（api-client）、a1de123（web abandon + 焦点修复）、状态同步与本条日志。

## 2026-09-07 09:20:35 +08:00 — favicon 支持系统深色模式换色

- 完成：在 SVG 内增加 `prefers-color-scheme: dark` 规则，深色模式下将左侧半环提亮，保留浅色模式原色、品牌蓝及全部路径几何；README 说明 favicon 跟随系统偏好，独立于页面主题切换。
- 涉及：`apps/web/public/favicon.svg`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：Chromium 实测浅色 → 深色 → 浅色计算填充色正确；16px / 32px 深浅背景预览通过；XML 解析与原路径 / viewBox 一致性检查通过；Impeccable detector 无命中，`git diff --check` 通过。

## 2026-09-07 07:37:26 +08:00 — 修正 favicon：从品牌 PNG 实际轮廓重新矢量化

- 完成：针对首版手工近似曲线导致右侧轮廓缩小时近似封闭 D、与品牌 PNG 不一致的问题，重新以 `docs/brand/logo.png` 为唯一几何来源：按颜色分离 PNG 中深色与蓝色两个连通实心轮廓，以 50% alpha 边界分别矢量化，再组合为无描边 SVG；颜色改用 PNG 主色 `#242a34` / `#1771e4`，保留原图的开口、内嵌 L、底部延伸和笔画比例，仅通过 viewBox 裁去外围透明留白。移除首版自行添加的系统深色换色，使品牌颜色与 PNG 一致；README / web AGENTS 的资产说明同步修正。
- 涉及：`apps/web/public/favicon.svg`、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：SVG 通过 XML 解析；将源 PNG 与 SVG 分别缩放到 16×16、32×32 后逐组并排检查，关键轮廓与开口位置一致；生产构建通过，打包后的 `dist/favicon.svg` 与源文件一致。

## 2026-09-07 07:32:32 +08:00 — 基于现有品牌 Logo 落地 web SVG favicon

- 完成：以 `docs/brand/logo.png` / `logo概念图.png` 的双色开环与内嵌 L 为视觉权威，为浏览器标签页重绘小尺寸友好的纯色几何 SVG；主蓝使用现有设计 Token 的 `#0072e2`，左侧深色半环在 `prefers-color-scheme: dark` 下自动提亮，透明背景兼容浏览器浅 / 深色界面。图标放入 Vite `public/favicon.svg`，`index.html` 增加 `type="image/svg+xml"` 的 favicon 声明。README 删除最后一条 favicon 待办并注明当前登记的 MVP 待办已全部完成，web AGENTS 状态段同步品牌资产现状。
- 涉及：`apps/web/public/favicon.svg`（新增）、`apps/web/index.html`、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：SVG 通过 XML 解析；分别栅格化为 16×16 与 32×32 做像素级预览，循环轮廓与 L 在标签页尺寸下保持可辨；Impeccable detector 无规则命中（环境缺少 HTML parser，按其提示退化为正则扫描）；`pnpm --filter @lexi-loop/web build` 通过，且 `dist/favicon.svg` 与源文件逐字一致。

## 2026-09-07 02:40:00 +08:00 — 实现 web README 剩余待办：服务端契约扩展（掌握度 / 优先级、导入逐词结果）与「再来一轮」行为定稿

- 完成：按 web README 待办落地全部三条（favicon 品牌产出除外）。① 契约扩展（先改权威文档）：`docs/api/words.md` §3–§4 列表 / 详情项新增 `masteryScore`（0–100 取整）与 `reviewWeight`（两位小数），公式权威仍在 review/algorithm.md（D011，服务端读取时动态计算）；§2 导入响应新增 `items` 逐词结果（`word` / `count` / `result: created|updated`），明确 items 为单词粒度、聚合统计为 user_words 行粒度（归一到同一新建词条的多个词形都记 created）。review-flow.md §4 优先级列示例改为权重数值并与实现一致；§9 定稿「再来一轮」——以上一轮实际词数（session 汇总 total）直接开始新一轮（D009 截断仍由服务端保证），开始失败留在结果页展示错误与重试。② 服务端：service `ImportWords` 输出逐词 outcome（词条 created 标志映射回输入词、按首次出现排序），`WordItem` 组装时计算 MasteryScore / ReviewWeight；DTO 层做契约舍入；重新生成 docs/openapi spec 并重生成 packages/api-client。③ 前端：生词库表格与详情统计展示掌握度（百分比）与优先级（权重数值）；导入页结果反馈新增逐词列表（「新增 / 已存在 +N」，review-flow §3）；结果页「再来一轮」直接开始并以快照一次性 `autoResume` 标记让 `/review` 跳过恢复询问直接进入第一题（进入即消费，刷新恢复仍由用户选择 D010），开始失败复用新抽出的 `utils/review-errors`（与复习页共用错误文案映射，结构化判定不依赖 api-client）。README 待办三条删除并同步完成情况，AGENTS.md 状态 / D011 条目 / 测试段更新。favicon 条目按用户要求保留未动。
- 涉及：`docs/api/words.md`、`docs/frontend/review-flow.md`、`docs/openapi/`（重生成）、`apps/server/`（service/word.go、handler/v1/dto/word.go、service 与 handler 测试）、`packages/api-client/`（重生成）、`apps/web/`（WordsPage / WordDetailPage / ImportPage / ReviewPage / ReviewResultPage、utils/review-snapshot + review-errors 及单测、tests/mocks/handlers 类型化工厂、tests/integration/{helpers,words-pages,import-page,review-result-page}.test.ts、README.md、AGENTS.md）、`docs/agent-log/2026-09.md`
- 验证：`apps/server` go build / vet / gofmt 无报错，`go test ./...` 全绿（含 testcontainers 集成用例：逐词结果、新词 mastery=50 / weight=6 与 7 的确定性、已复习词动态计算）；`pnpm -F @lexi-loop/api-client generate` 后 `test:run`（24 用例）与 `typecheck` 通过；`pnpm -F @lexi-loop/web lint` / `format` 无报错、`test:run` 27 文件 151 用例全绿（web jsdom 137 + storybook Chromium 14，新增页面集成 5 用例）、`build`（vue-tsc -b + vite）与 `test:e2e`（Playwright 2 用例）通过。按需提交 6 个：c5f046f（权威文档）、d33d1c8（服务端 + spec）、9026aae（api-client 重生成）、ced8cf1（生词库 / 详情展示）、ca039bd（导入逐词反馈）、dbbd7be（再来一轮）。

## 2026-09-07 01:55:00 +08:00 — 落地 web 剩余质量设施（ESLint + Prettier / 组件测试环境 / MSW / Storybook 测试与 a11y 门禁 / Playwright）

- 完成：按 web README 待办「引入剩余质量设施」一次落地五件（《前端技术栈》§10/§11、《前端测试规范》§15）。① ESLint Flat Config（`eslint.config.js`）：`@vue/eslint-config-typescript` 的 `recommendedTypeChecked` 类型感知规则 + `eslint-plugin-vue` flat/recommended + `eslint-plugin-vuejs-accessibility` + `@tanstack/eslint-plugin-query` + `eslint-plugin-import-x`（typescript resolver 解析 `@/`，`import-x/parsers` 指定 vue-eslint-parser 处理 .vue），末尾 `eslint-config-prettier` 关闭格式冲突；`label-has-for` 改为规则文档的 `some: ['nesting','id']` 语义（默认 every 误报 for + 兄弟控件写法），`src/components/ui/**` 按 §8 单词命名豁免 `vue/multi-word-component-names`；随首次 lint 修复 review-snapshot 两处非必要类型断言、Dialog 属性顺序、ReviewPage 键盘捕获容器（程序化焦点目标）行内豁免。② Prettier（`.prettierrc.json` + `.prettierignore`）：`semi: false / singleQuote: true` 对齐既有风格，`prettier-plugin-tailwindcss` 以 `src/styles/main.css` 为样式表做 class 排序，全量格式化 apps/web（Markdown 表格对齐属正常 churn）。③ 组件测试环境：`vitest.config.ts` 按测试规范 §15 mergeConfig vite 配置，jsdom + `tests/setup.ts`（jest-dom matcher + Testing Library cleanup + Vitest 隐式清理项），coverage 换 `@vitest/coverage-v8` 4.x（首装 5.x 与 vitest 4.1 peer 不匹配已降级），`tests/` 与 `vitest.config.ts` 纳入对应 tsconfig 供类型感知 lint 与 `vue-tsc -b` 检查；Button 组件测试 4 用例随设施落地。④ MSW：`tests/mocks/`（空默认 handler 工厂 + `setupServer`，只从同构入口 'msw' 导入），setup 统一 listen/reset/close 且 `onUnhandledRequest: 'error'`；vitest `test.env` 注入 `VITE_API_BASE_URL=http://lexi-loop.test`（保留假主机，node/jsdom fetch 不接受相对路径），集成冒烟 `tests/integration/api-client.test.ts` 走真实生成端点 → mutator → transport 链路验证 envelope 解包与 ApiError 转换（`env.test.ts` 的「未设置时为空串」用例改为显式 stub 空串）。⑤ Storybook addon：CLI 安装 `@storybook/addon-a11y` + `@storybook/addon-vitest`（CLI 在 Playwright 询问处中断，vitest project 接线按包内 `vitest.config.4` 模板手工并入 vitest.config.ts——`storybookTest()` 插件 + `@vitest/browser-playwright` provider + chromium headless，两 project 均 `extends: true` 继承根级 vue / tailwind 插件与别名，jsdom 设施只挂 web project；CLI 写入的 `playwright: latest` 锁回 ^1.63.0）；preview.ts 全局 `a11y.test: 'error'` 门禁，抓出并以菜单打开收尾的两个 Story（ThemeSwitcher DarkMenuOpen / DropdownMenu RadioGroup）对 `aria-hidden-focus` 一条规则豁免——Reka 弹层打开时给根节点标 aria-hidden、焦点圈闭在层内，axe 静态分析识别不了圈闭，属误报，注释登记。⑥ Playwright：`playwright.config.ts` webServer 跑 `pnpm build && pnpm preview --host 127.0.0.1`（vite preview 默认只绑 IPv6 localhost，需显式 host），`e2e/smoke.spec.ts` 2 用例（重定向 /review + 外壳语义、主导航跳生词库），vite.config 补 preview 代理与开发环境对齐。README 待办「质量与品牌」质量设施条目删除（favicon 条目保留）并改为已就绪说明，命令表补 lint / format / test:coverage / test:storybook / test:e2e；AGENTS.md 状态段与测试段同步（组件 / 页面集成测试自此按测试规范 §4 优先级随实现补充，不再有「设施未落地」豁免）。
- 涉及：`apps/web/`（eslint.config.js、.prettierrc.json、.prettierignore、vitest.config.ts、playwright.config.ts、tests/{setup.ts,mocks/,integration/}、e2e/smoke.spec.ts、package.json、tsconfig.{app,node}.json、.gitignore、vite.config.ts、.storybook/{main,preview}.ts、全部源码 prettier 格式化、Button 组件测试、ThemeSwitcher / DropdownMenu Story a11y 豁免、review-snapshot / Dialog / ReviewPage lint 修复）、`apps/web/README.md`、`apps/web/AGENTS.md`、`pnpm-workspace.yaml`（allowBuilds 补 unrs-resolver / msw）、`pnpm-lock.yaml`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web lint` / `format` 无报错；`test:run` 23 文件 137 用例全绿（web jsdom 123 + storybook Chromium 14）；`test:coverage`（v8）与 `test:e2e`（Playwright 2 用例，生产构建 + preview）通过；`build`（vue-tsc -b + vite）与 `storybook:build` 通过；`typecheck` 依旧仅作辅助（历史结论以 build 为准）。按需提交 5 个：3910840（ESLint + Prettier）、dacf589（组件测试环境）、75cdfea（MSW）、69f4f35（Storybook addons）、f52d7f3（Playwright）。

## 2026-09-07 01:08:00 +08:00 — 登记「再来一轮」行为定义为 web 体验待办

- 完成：按用户要求把浏览器回归中的观察登记为待办——复习结果页「再来一轮」当前回到 `/review` 数量配置页（保留上次选择），review-flow §9 未定义点击后的走向，是否改为按上一轮配置直接开局待定；README 待办新增「体验待办」小节承载该条目，并注明落地前须先在 review-flow.md §9 定稿行为再同步实现。无代码变化。
- 涉及：`apps/web/README.md`（提交 ff160f8）、`docs/agent-log/2026-09.md`
- 验证：条目与回归报告结论一致；小节位置遵循 README 待办「按依赖顺序登记、完成后删除」的既有结构。

## 2026-09-07 01:05:00 +08:00 — 复习流程浏览器回归：修复自定义数量输入冻结配置卡片

- 完成：对 /review 全流程做真实浏览器回归（开始 / D009 截断提示 / recalling → revealed → answered 状态机 / Space 1 2 ← → 键盘 / 恢复与放弃 D010 / 结果页统计与深链 / 不放回抽样 / 连按防重 / 不存在 session 404 态），主链路全部通过；发现并修复一个功能缺陷——自定义数量输入任意数字后「确认」按钮永久禁用、预设与提示渲染冻结。根因：`v-model` 在 `type="number"` 输入上经 `looseToNumber` 把可解析输入写回为 number，模板 `customCountInput.trim()`（确认按钮 disabled 绑定）与 `applyCustomCount` / `handleStart` 内的 `.trim()` 抛 TypeError 中断渲染与开始流程。修复为 `:value` + `@input` 显式存原始字符串，使 ref 类型恒为 string，并在输入处理函数注释说明不可改回 v-model 的原因。另按交互与可访问性规范 §10.2 给预设数量按钮补 `aria-pressed` 选中态语义（原先只靠 secondary 变体颜色表达）。「再来一轮」回到数量配置页（而非按原配置直接开局）评估为文档未定义的合理 UX，不改行为仅记录。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`（提交 1d6c295 修复输入类型、a2e3959 补 aria-pressed）、`docs/agent-log/2026-09.md`
- 验证：`pnpm typecheck` 通过、`pnpm test:run` 16 文件 117 用例全绿；浏览器复验修复链路——输入 3 解禁确认、应用后 D009 提示消失、按 3 开局 session total_count=3 并完整作答跳转结果页（统计 1 记得 / 2 不记得 / 33% 与需要加强列表一致）。备注：测试环境为 Vite dev + 本地后端 + podman PG（预置 6 个含释义生词）；回归中 In-app Browser 的合成鼠标 / CUA 输入管线在页面刷新后失效（新标签页同样），后半段验证改用 fill / 键盘 / DOM 级 click 完成，非应用缺陷。

## 2026-09-07 00:29:57 +08:00 — 完成 web 纯逻辑单元测试待办（复习状态机迁移 / 键盘映射）

- 完成：README 待办「纯逻辑单元测试」收尾——把 ReviewPage.vue 内联的最后两块纯逻辑抽为 `src/utils/` 纯函数并补单测。① `utils/review-state-machine.ts`：`ReviewMode` 类型与三个迁移函数——`enterReview(itemCount, resumeIndex?)`（开始 / 恢复共用入口，恢复下标钳制到界内、对齐失败回退第 1 题、空轮防御分支保持 idle）、`revealCard`（仅 recalling 且有当前卡迁入 revealed，否则 null 不迁移）、`advanceAfterAnswer`（答对答错通用推进：下一张 recalling 或 finished 跳结果页）；页面保留 ref 状态与副作用（提交 / 焦点 / 路由），作答入口守卫（revealed / 非提交中 / 有 session）仍在页面。② `utils/review-keyboard.ts`：`resolveReviewKeyAction` 把 `{key, code}` + 模式 + 「焦点是否在控件上」解析为 reveal / answerForgotten / answerRemembered 或 null（不拦截）——idle 无快捷键、Space 揭示（revealed 下仍映射由页面迁移守卫吸收，行为与抽离前一致：拦截避免滚动）、控件上的 Space 让位原生激活而 1 / 2 仍作答；DOM 归约（`closest` 判定控件）留在页面。README 待办按「完成后删除条目」约定移除该行，结构段 utils 清单补全（原清单缺 review-snapshot / review-resume / isPageLevelNavigation，一并列出）。
- 涉及：`apps/web/src/utils/review-state-machine.ts` 及 `.test.ts`（新增，11 用例）、`apps/web/src/utils/review-keyboard.ts` 及 `.test.ts`（新增，9 用例）、`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `build`（vue-tsc -b + vite）通过；`test:run` 16 文件 117 用例全绿（新增 20 用例）。迁移与映射为纯函数等价替换（快捷键作用域、focusReviewArea 时机、恢复钳制、最后完成跳转等行为不变）；组件 / 页面集成测试仍待质量设施落地（README 质量与品牌条目已登记），建议下次浏览器过一遍复习页键盘链路（Space / 1 / 2 / ← / → 与控件焦点让位）作人工回归。

## 2026-09-07 00:16:00 +08:00 — 实现 SPA 路由导航后焦点管理

- 完成：README 待办「SPA 路由导航后焦点管理」（交互与可访问性规范 §5.3）。页面级路由导航完成后把焦点移到新页面主标题——各路由页 h1 补 `tabindex="-1"`，`src/app/router.ts` 的 `afterEach` 在 `document.title` 同一处管理焦点移动（`nextTick` 等新页面挂载后聚焦，兜底目标为 `#main-content` 主内容入口）；判定逻辑抽为纯函数 `utils/isPageLevelNavigation`：初次加载（from 为 START_LOCATION）与仅 query / hash 变化（搜索、分页、筛选等局部 URL 更新）不移动焦点，路径 / 路由参数变化才算页面级导航。README 待办移除该条目并同步页面基础设施描述与纯逻辑测试清单。
- 涉及：`apps/web/src/utils/isPageLevelNavigation.ts` 及 `.test.ts`（新增）、`apps/web/src/app/router.ts`、六个路由页面 h1 加 `tabindex="-1"`（`pages/error/NotFoundPage.vue`、`pages/import/ImportPage.vue`、`pages/review/ReviewPage.vue`、`pages/review/ReviewResultPage.vue`、`pages/words/WordDetailPage.vue`、`pages/words/WordsPage.vue`）、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm typecheck`、`pnpm test:run`（14 文件 97 用例全绿，含 isPageLevelNavigation 4 用例）、`pnpm build` 通过；另以真实浏览器（Vite dev + 本地后端）冒烟：初始加载焦点留在 body；点击导航到导入页后 `document.activeElement` 为「导入生词」h1 且 title 同步；搜索提交仅 query 变化焦点保持在搜索框；浏览器后退跨页面边界焦点移到新页 h1、后退至同路径仅 query 变化不动焦点。

## 2026-09-06 23:54:40 +08:00 — 复习流程审查修复（恢复检查 / 恢复进度 / D009 提示 / 快捷键作用域 / skip link）

- 完成：审查复习页与页面基础设施两个会话的落地后修复 10 项。①恢复检查改走 Feature 层——`reviewSessionQueryOptions`（review）/ `wordsListQueryOptions`（words）作为命令式取数与 useQuery 共用的 options 工厂导出，ReviewPage 的 active session 检查从裸 `fetch` + `import.meta.env` 改为 `queryClient.fetchQuery`（staleTime: 0 强制取新），接受统一 transport 的超时 / abort / envelope 校验与 ApiError 分类（架构规范 §4/§15：Page 不得直接调用 fetch 或拼接 API URL）。②瞬时失败不再销毁恢复入口——检查失败按 ApiError 区分：404（session 已不存在）清快照回 idle，网络 / 超时 / 5xx 保留快照并新增「检查复习进度失败，请重试」错误态；「放弃本轮」与清理逻辑收拢为 `discardSnapshot`。③恢复进度真正恢复——新增 `utils/review-resume.ts` 纯函数 `computeResumeProgress`（GET session 逐词结果 `{word, result}` 与本地快照按 word 对齐，word 一轮内唯一；长度不等 / 重复 / 未对齐返回 null），「继续复习」跳到首个未答项续答（review-flow §6「回到原进度」），对齐失败回退第 1 题（已答题重复提交经服务端幂等短路 D005 不重复计数）；修复恢复后模板分支仍命 recoveryState='recoverable' 导致闪卡永不显示的问题（恢复完成置 idle）。④D009 提示开始前生效——可用量取 GET /words（pageSize 1）分页 total（与复习候选集同为未删除生词），取不到不提示；原 `totalCount` 只在开始后赋值导致提示永不显示的死逻辑删除。⑤键盘快捷键作用域——keydown 从 document 级监听改为绑定复习区域元素（§5.4 只在目标控件聚焦时生效），焦点在按钮 / 链接等控件上时 Space / Enter 保留原生激活；揭示释义后焦点移回复习区域（查看释义按钮卸载不再丢焦到 body）。⑥自定义数量输入未确认直接开始时先应用输入值，不再按旧预设开局。⑦开始失败错误文案按错误分类映射（无可复习生词 / 网络 / 5xx / 兜底），不透传服务端英文 message。⑧结果页：不存在 id（ApiError 404）呈「没有找到这轮复习」态（对齐详情页），未完成 / 已放弃分支区分文案，错误文案同样按分类映射。⑨快照 items 校验收紧（itemId / word 字段，畸形数据整体回退 null）并补 3 用例；删除 `answeredCount` 死代码与永不触发的释义兜底文案；`handleAnswer` 改用内存 sessionId 不再逐次读 localStorage。⑩AppShell skip link 目标 `main` 补 `tabindex="-1"`（跳转后焦点真正落入主内容）。README 待办补登「SPA 路由导航后焦点管理」（§5.3，未实现）。
- 涉及：`apps/web/src/features/review/api/queries.ts`、`apps/web/src/features/words/api/queries.ts`、`apps/web/src/utils/review-resume.ts` 及 `.test.ts`（新增）、`apps/web/src/utils/review-snapshot.ts` 及 `.test.ts`、`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/src/pages/review/ReviewResultPage.vue`、`apps/web/src/components/layout/AppShell.vue`、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `build`（vue-tsc -b + vite）/ `test:run`（93 用例，新增 review-resume 对齐 9 用例与快照畸形 items 3 用例）通过。备注：恢复流程 / 快捷键的真实后端浏览器端到端核对未执行（本环境合成点击受限，且组件级测试设施未引入，见 README 待办），行为正确性以纯函数单测 + vue-tsc 模板类型检查为准，建议下次登录页面时人工过一遍「中断 → 恢复 → 续答 → 完成跳转」链路。

## 2026-09-06 23:17:00 +08:00 — 落地 web 页面基础设施（路由 meta title / skip link / main landmark）

- 完成：按《前端交互与可访问性规范》§6.1 落地三项页面基础设施。① 路由 meta → `document.title`：`router.ts` 每条路由新增 `meta.title`（品牌后缀 `· LexiLoop`），`router.afterEach` 统一在导航后设置 `document.title`；ImportPage / WordsPage / ReviewPage / ReviewResultPage 移除各自的 `watchEffect` 页面标题代码（含未使用 `watchEffect` import 清理），WordDetailPage 保留动态标题覆盖（`detail.value.word` 异步获取后替换默认标题）。② AppShell skip link：`<a href="#main-content">` 位于 DOM 最前，`sr-only focus:not-sr-only` 视觉隐藏 + Tab 聚焦时显示（蓝色胶囊按钮），焦点环按设计系统方案 §8.1 配方；`<main>` 增加 `id="main-content"` 作为 skip link 目标与唯一主内容 Landmark。③ 各页面 h1 与 Loading / Empty / Error 分支审查：全部六个路由页面已具备 h1 与对应状态分支（导入页为表单页，无异步加载态；其余页面均有骨架 / 错误重试 / 空态分支）。README 待办勾选「页面基础设施」。
- 涉及：`apps/web/src/app/router.ts`、`apps/web/src/components/layout/AppShell.vue`、`apps/web/src/pages/{import/ImportPage,words/WordsPage,review/ReviewPage,review/ReviewResultPage}.vue`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck`（vue-tsc --noEmit）/ `build`（vue-tsc -b + vite）/ `test:run`（81 用例）通过。

## 2026-09-06 22:53:00 +08:00 — 落地 web 复习页 `/review` 与复习结果页 `/review/result/:session`（review-flow §6–§10）

- 完成：替换 `pages/review/ReviewPage.vue` 与 `ReviewResultPage.vue` 占位为完整业务实现。ReviewPage（review-flow §6–§8、§10）：数量选择（10 / 20 / 30 / 50 预设按钮 + 自定义正整数输入，超出词库时按 D009 提示截断但不阻止开始）；active session 恢复（D010）——进入时读 localStorage 快照 + fetch 服务端 session 状态，active 时展示「继续复习 / 放弃本轮」，completed 时跳转结果页，abandoned / 失败时清快照回 idle；状态机 idle → recalling → revealed → answered → 下一题 recalling 或完成跳转结果页；键盘操作 Space 揭示释义、1 / ← 不记得、2 / → 记得（§10），焦点在复习区域 div（tabindex=-1）确保键盘事件被捕获；提交失败保持当前状态供重试。ReviewResultPage（review-flow §9）：`useReviewSessionQuery` 拉取 session 数据，汇总（总计 / 记得 / 不记得 / 正确率百分比），forgotten 单词列表（「需要加强」），操作按钮「再来一轮」（router push review）与「回到生词库」；Loading 骨架 / Error 重试 / 未完成态分支。新增 `utils/review-snapshot.ts`（localStorage 快照存取，schema version 守卫，版本 / 字段校验）及 11 个单测（save / load / clear / 各种无效数据回退 null）。README 待办勾选复习页与复习结果页，`apps/web/AGENTS.md` 状态段与单测覆盖清单同步。
- 涉及：`apps/web/src/pages/review/ReviewPage.vue`、`apps/web/src/pages/review/ReviewResultPage.vue`（替换占位）、`apps/web/src/utils/review-snapshot.ts`（新增）、`apps/web/src/utils/review-snapshot.test.ts`（新增，11 用例）、`apps/web/README.md`、`apps/web/AGENTS.md`
- 验证：`pnpm -F @lexi-loop/web build`（vue-tsc -b + vite）/ `test:run`（81 用例）/ `typecheck` 通过。

## 2026-09-06 22:14:00 +08:00 — 落地 web 生词详情页 `/words/:id`（review-flow §5）

- 完成：新增 `pages/words/WordDetailPage.vue` 并注册 `/words/:id` 路由（name `word-detail`），生词库行内单词 + 音标改为 RouterLink 打开详情（真实链接保留中键 / 新标签页）。页面结构：返回生词库链接 → h1 单词 + 音标 → 生效释义逐条展示（D007 三层取值由服务端 `effectiveReviewMeaning` / `meaningSource` 表达，`custom` 时标注「自定义」Chip；空释义给「编辑复习释义」引导）→ 分隔线 → 统计 dl（遇到 / 复习 / 记得 / 忘记 / 上次复习，无掌握度 / 优先级——D011 契约缺口）→ 操作区（编辑复习释义 secondary、删除生词 danger-soft）。编辑复习释义 Dialog：新增 `ui/dialog`（Dialog / DialogClose）与 `ui/alert-dialog`（AlertDialog / AlertDialogCancel / AlertDialogAction）基础组件（Reka 组合 + §8.3 弹层配方 + Forced Colors outline 兜底，受控 `open` 经 `update:open` 交给页面裁决，Dialog 支持 `data-dialog-initial-focus` 覆写 Reka 默认的「聚焦首个可聚焦元素」，AlertDialog 依赖 Reka 语义不响应 Esc / 点击遮罩，初始焦点落在前置的取消按钮）；编辑器文本 ⇄ `Meaning[]` 由新纯函数 `utils/meaningText.ts` 编解码（每行一条义项「pos. 译文；译文」，词性前缀需「. 」且不允许前缀内含点，`3.14` / `U.S.` 不误拆，往返一致），预填为当前生效释义、未变化时禁用保存（避免无改动误存自定义），清空保存 = PATCH null，`custom` 时另有「清除自定义释义」直达；提交中拦截关闭（§7.2），失败聚焦对话框内错误摘要（§5.3）。删除生词 AlertDialog：描述含具体单词与后果、恢复语义（D004 软删除），确认按钮不复用 AlertDialogAction 的自动关闭而由页面在成功后关闭并 `router.push('/words')`；已删 / 不存在 id（ApiError 404）呈「没有找到这个单词」态（回到生词库 / 去导入），其余错误为页面级 Error + 重试。`utils/formatDateTime.ts` 把 `lastReviewedAt` 按东八区渲染为「YYYY-MM-DD HH:mm」（空 / 非法取 null 显示「—」）。`document.title` 随详情更新为「单词 · LexiLoop」。README 待办勾选生词详情、评估段 / 结构段 / 纯逻辑单测条目同步，`apps/web/AGENTS.md` 状态段与单测覆盖清单同步。
- 涉及：`apps/web/src/pages/words/WordDetailPage.vue`（新增）、`apps/web/src/app/router.ts`、`apps/web/src/pages/words/WordsPage.vue`（行内链接）、`apps/web/src/components/ui/dialog/`（Dialog / DialogClose / Story，新增）、`apps/web/src/components/ui/alert-dialog/`（AlertDialog / Cancel / Action / Story，新增）、`apps/web/src/components/ui/index.ts`、`apps/web/src/utils/`（meaningText / formatDateTime 及两个 `.test.ts`，新增）、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web build`（vue-tsc -b + vite）/ `test:run`（70 用例）/ `typecheck` 通过；发现 `pnpm typecheck` 跑在 solution tsconfig（`files: []`）上不检查任何文件，真实门禁是 build，组件 typing 问题（未使用 import、Story 必填 props 强制 args）已修复。真实后端（PostgreSQL + 种子词典）+ vite dev 浏览器核对：列表点单词进详情、统计与三层释义展示（review / raw 回退 / 自定义 badge）、编辑对话框预填与初始焦点在 textarea、未修改禁用保存、保存后 badge 与列表同步、清除自定义回退词典层、删除确认（初始焦点在取消、取消留在原页、确认后回列表且行消失）、已删 id 呈 404 态、`document.title`、浅 / 深主题截图（详情页 / 对话框 / badge / danger-soft）核对无误。备注：本环境 IAB 合成点击（locator click）仍超时，交互沿用页面内 DOM click 验证；本地 dev 库为 `localhost:5432/lexi_loop`（角色 lexi），验证数据为 ambiguous / constrain / derive 三条。

## 2026-09-06 20:38:00 +08:00 — 落地 web 生词库页 `/words`（review-flow §4）

- 完成：替换 `pages/words/WordsPage.vue` 占位为完整生词库列表。可分享状态写入 URL（架构规范 §6.2）——`page` / `search` 权威来源是路由 query（`parsePositiveInt` 解析、非法回退默认值，默认值不写入 URL），搜索提交（表单 submit / Enter）才落 URL、输入中数值留本地，`watch` 使浏览器后退 / 前进把已提交搜索词同步回输入框；页码 / 搜索变化 `router.push` 进历史。语义表格按 review-flow §4 列（单词 + 音标 / 释义 / 遇到 / 复习 / 记得 / 忘记，数字列右对齐 `tabular-nums`、表头 `whitespace-nowrap`），释义列用 `formatMeanings`（「pos. 译文」单行，空取「—」）；无掌握度 / 优先级列（D011 契约缺口）。状态分支：Loading 用骨架（aria-hidden + `role="status"` 播报）、Error 用 `role="alert"` + 重试（`:pending` 复用 isFetching）、Empty 区分空词库（引导导入，RouterLink 主按钮样式）与搜索无结果（清除搜索）；汇总行 `role="status"` 预存 DOM 播报「共 N 个单词 · 第 x / y 页」。分页控件为 Navigation Landmark，页码按钮 `aria-current="page"`、上一页 / 下一页 Chevron 图标按钮带 `aria-label`、越界原生 disabled；页码窗口由 `buildPageItems` 生成（端点恒在、连续缺口折叠省略号，越界钳制）。`useWordsListQuery` 补 `placeholderData: keepPreviousData`（翻页 / 改搜索不闪回骨架，isFetching 标记刷新）。新增三个纯函数 utils 及单测：`buildPageItems`（8 用例）、`parsePositiveInt`（5 用例）、`formatMeanings`（7 用例，入参为结构化局部类型、不依赖 api-client）。README 待办勾选生词库、评估段与结构段 / 纯逻辑单测条目同步，`apps/web/AGENTS.md` 状态段同步。
- 涉及：`apps/web/src/pages/words/WordsPage.vue`、`apps/web/src/features/words/api/queries.ts`、`apps/web/src/utils/`（buildPageItems / parsePositiveInt / formatMeanings 及三个 `.test.ts`，均新增）、`apps/web/README.md`、`apps/web/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `test:run`（51 用例）/ `build` 通过。真实后端 + vite dev 浏览器核对：翻页写 URL 与 `aria-current` 迁移、浏览器后退恢复列表状态、搜索「模棱」按释义命中并回显已提交词、后退清空输入框、无结果空态与「清除搜索」、停库触发 500 后 Error 态文案 + 重试按钮且重启库后重试恢复列表、无释义行显示「—」；浅 / 深主题截图核对（发现并修复两处布局问题：搜索按钮被挤压致文字竖排，输入框补 `min-w-0 flex-1`、按钮补 `shrink-0`；数字列表头窄列竖排，补 `whitespace-nowrap`）。备注：本环境 IAB 合成点击（locator click / force / cua / dom_cua）全部超时，交互验证改用页面内 DOM click（同样触发 Vue 原生事件监听），视觉以截图为准；Loading 骨架因本地 API 过快未截到，仅静态核对。

## 2026-09-06 19:50:00 +08:00 — 建立 apps/web 子树 AGENTS.md

- 完成：新增 `apps/web/AGENTS.md`，作为 web 子树内 Agent 的补充指引，覆盖当前骨架状态（导入页已落地、其余页面占位）、权威文档速查表、常用命令（含 api-client 重新生成）、架构与实现要点（含常见踩坑的文档小节指针，§ 编号已逐一核对）、MVP 边界与冻结决策（web 侧）、变更时的文档同步纪律；只引用不复制 schema / 公式 / 决策正文。根 `AGENTS.md` 导航段补 web 子树指引；顺带修正设计系统文档的过时引用——实际文件为 `Vue 组件设计系统方案.md`，原「Reka UI + Tailwind CSS v4 + Radix Colors 设计系统方案.md」不存在（`apps/web/README.md` 等下游早已用新名），涉及根 `AGENTS.md`（权威表 + 冲突裁决）、`docs/README.md`（目录树 + 权威关系表）与 `docs/specs/frontend/前端技术栈.md`（2 处 wiki 链接）。
- 涉及：`apps/web/AGENTS.md`（新增）、`AGENTS.md`、`docs/README.md`、`docs/specs/frontend/前端技术栈.md`、`docs/agent-log/2026-09.md`
- 验证：脚本核对新文档内全部相对链接均指向存在的文件；`grep` 全仓库无旧文件名残留（agent-log 历史记录除外）；命令与 `apps/web/package.json`、`packages/api-client/package.json` scripts 一致。

## 2026-09-06 19:23:00 +08:00 — 导入页审查修复（焦点管理 / parseImportText 归位 / 文档同步）

- 完成：审查上次导入页落地后修复 5 项。①焦点管理按《前端交互与可访问性规范》§5.3 补齐——导入成功后把焦点移到「导入完成」h2（触发按钮随视图切换卸载，避免丢到 body，同时解决 role="status" 区域随内容新插入导致播报不可靠的问题），提交失败后移到错误摘要 `<p role="alert">`（临时 `tabindex="-1"` + `nextTick` 后 focus）。②`parseImportText.ts` 及其测试从 `features/words/` 移到 `src/utils/`（§4.5「纯函数放 utils/」字面要求，git mv 保留历史）。③修正 ImportPage 页面标题注释的错误引用（review-flow 无 §6.1，改为《前端交互与可访问性规范》§6.1 并注明路由 meta 基建落地前由页面自行设置）。④README 同步：评估段改为「导入页已落地，其余仍为占位卡片」、结构段补 `src/utils/`、纯逻辑单测条目注明导入解析聚合已完成。⑤parseImportText 补 CRLF（`\r\n`）用例锁定 trim 兜底行为（现 10 用例）。
- 涉及：`apps/web/src/pages/import/ImportPage.vue`、`apps/web/src/utils/parseImportText.ts` 及 `.test.ts`（自 `features/words/` 移入）、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web test:run`（32 用例）/ `typecheck` / `build` 通过。

## 2026-09-06 19:02:00 +08:00 — 落地 web 导入页 `/import`（review-flow §2–§3）

- 完成：新增 `features/words/parseImportText.ts` 纯逻辑解析函数（trim → lowercase → 去空行 → 重复单词聚合成 `{ word, count }[]` 保留首次顺序），配套 9 个单元测试覆盖空输入、空白行、大小写合并、顺序保留与尾换行。替换 `pages/import/ImportPage.vue` 骨架为完整导入流程：textarea 多行输入 + 实时解析预览（总遇词次数 + 不同单词数）→ `useImportWordsMutation` 提交（pending / error 状态）→ 成功后展示聚合反馈（新增 / 已有 / 遇到次数）+ "继续导入" 与 "回到生词库" 导航；页面标题 `watchEffect` 更新、`<label>` 关联 textarea、`role="status"` live region 播报结果。README 待办条目标记完成。
- 涉及：`apps/web/src/features/words/parseImportText.ts`（新增）、`apps/web/src/features/words/parseImportText.test.ts`（新增）、`apps/web/src/pages/import/ImportPage.vue`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `build` / `test:run`（31 用例）通过。

## 2026-09-06 18:32:00 +08:00 — 落地 web Feature 层（words / review 的 api keys / queries / mutations）

- 完成：按《前端应用架构规范》§8.1 新建 `features/words/api/` 与 `features/review/api/`，把 8 个 api-client 端点包装为 Feature 业务能力，DTO 直接使用生成类型、queryFn 透传 `signal`。Query Key 工厂集中键层级（words：`['words', 'list', params]` / `['words', 'detail', id]`；review：`['review', 'sessions', sessionId]`），查询入参接受 `MaybeRefOrGetter`（列表搜索 / 分页、路由 param 响应式），queryKey 用 computed 建键随参数自动重新取数。words mutations：importWords 全量失效 `wordsKeys.all`（响应无受影响 id 且重复导入会累计 encounterCount）、updateWordReviewMeaning 失效对应 detail + lists（自定义释义参与 D007 三层取值，两处 effectiveReviewMeaning 都变）、deleteWord 移除 detail 缓存（软删除 D004 后 GET 详情 404）并失效 lists。review mutations：startReviewSession 不失效既有缓存（服务端自动 abandon 旧 active，D010；返回 items 由页面落地快照）、submitReviewResult 失效 session 汇总（提交幂等 D005，结果页取最新 remembered / forgotten）。新增两个 keys 层级纯逻辑单测锁定失效契约。
- 涉及：`apps/web/src/features/words/api/`（keys.ts、queries.ts、mutations.ts、keys.test.ts，均新增）、`apps/web/src/features/review/api/`（同上四件，均新增）、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `build` / `test:run`（22 用例）通过；README 待办删除 Feature 层条目并把结构段更新为 features/ 现状。

## 2026-09-06 18:18:00 +08:00 — 抽出 ui/dropdown-menu 基础组件并让 ThemeSwitcher 复用

- 完成：把 ThemeSwitcher 内联的 Reka DropdownMenu 结构抽为 `components/ui/dropdown-menu/` 基础组件（设计系统方案 §8.3 Dropdown / Popover 与 Menu Item 配方 + §9 Reka 状态接入）。`DropdownMenu.vue` 固定 Root + Trigger + Portal + Content 组合，Content 套弹层配方（不透明 Overlay、Forced Colors 下带 `!` 后缀的容器 outline），`align`（默认 end）/ `sideOffset`（默认 8）为 props，触发器经 as-child 交给 `#trigger` 插槽（调用方负责可聚焦与可访问名称，通常是 ui/button 或图标按钮）；`DropdownMenuItem.vue` / `DropdownMenuRadioItem.vue` 落 Menu Item 配方（`data-highlighted` 高亮、`outline-hidden` 后 `focus-visible` 恢复焦点环、`data-disabled` 降效），Item 的 select / disabled 与 RadioItem 的 value 声明式透传给 Reka，RadioItem 内置 Check 指示图标并暴露 `group/item` 命名组供插槽图标响应选中态。RadioGroup 无样式纯语义，不包一层，调用方直接组合 reka-ui 的 `DropdownMenuRadioGroup`（ThemeSwitcher 与 Story 均如此）。新增 `DropdownMenu.stories.ts`（title `UI/DropdownMenu`）：Default（ui/button 触发器 + 普通 Item，play 验证打开 → select 关闭菜单与事件透传 spy）、RadioGroup（三选一偏好形态，play 验证选择后重开 menuitemradio 的 aria-checked 语义）；Portal 挂 body 的元素用 `within(document.body)` 查询。`ui/index.ts` 追加三个导出；ThemeSwitcher 改为组合 ui 组件，删除内联配方类与 Check 指示图标，触发器 ghost 图标按钮与外观保持不变。
- 涉及：`apps/web/src/components/ui/dropdown-menu/`（DropdownMenu.vue、DropdownMenuItem.vue、DropdownMenuRadioItem.vue、DropdownMenu.stories.ts，均新增）、`apps/web/src/components/ui/index.ts`、`apps/web/src/components/layout/ThemeSwitcher.vue`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `build` / `test:run`（16 用例）/ `storybook:build` 通过（`vue-tsc --noEmit` 再次漏报 stories 未使用导入与 RadioItem 必填 value 缺失，以 build 为准修掉：story 顶层 `userEvent` 导入被 play 参数遮蔽、RadioItem 显式声明 `value: AcceptableValue` 透传）；真实应用浏览器核对（vite dev）：一次点击打开菜单、初始勾选与 store 偏好一致、选「浅色」后菜单关闭且 html 主题类换入 light、localStorage 偏好持久化，菜单视觉配方（overlay / 圆角 / 阴影 / 右对齐 / 选中指示与选中图标主色）与重构前一致；Storybook 静态页核对 Default 与 RadioGroup 两 Story 的打开 / 选择 / 关闭交互与 aria 语义。备注：自动化合成点击（Playwright locator click）对「pointerdown 即开 + Portal 挂 body」的触发器会因命中目标复检失败而反复重试，验证以真实鼠标输入为准。

## 2026-09-06 17:33:34 +08:00 — 主题切换改为 Reka DropdownMenu + Lucide 图标组件

- 完成：应用户反馈替换 AppShell 头部的原生 `<select>` 主题切换。新增 `components/layout/ThemeSwitcher.vue`（读 theme store 的布局组件，非 `components/ui/` 通用件）：Reka DropdownMenu 弹层按设计系统方案 §8.3 配方（`rounded-popover bg-overlay p-1 shadow-overlay`、align end / side-offset 8、`max-h-(--reka-dropdown-menu-content-available-height)`、Forced Colors 下带 `!` 后缀补容器 outline），菜单项用 DropdownMenuRadioGroup / RadioItem 表达三选一偏好并绑定 store 的 `update:modelValue`，选中态由 DropdownMenuItemIndicator 勾选图标 + `menuitemradio` 语义 + 选中项图标主色（`group-data-[state=checked]/item:text-primary-text`）共同表达；触发器为 44×44（size-11，满足可访问性规范目标尺寸）ghost 图标按钮，按当前偏好显示 Sun / Moon / Monitor（`lucide-vue-next`，《前端技术栈》§9.3 指定图标库，新依赖），`data-[state=open]` 高亮与完整焦点环。AppShell 删除 select 与选项处理逻辑，换为 `<div class="ml-auto"><ThemeSwitcher /></div>`（DropdownMenuRoot 不渲染 DOM，布局由调用方包裹）。`.storybook/main.ts` 的 `viteFinal` 补 `@/` → `src` 别名（与应用 vite.config.ts 一致，Story 可引用 src 模块，兼容 SB 内部已有数组 / 对象两种 alias 形态）。新增 `ThemeSwitcher.stories.ts`（title `Layout/ThemeSwitcher`）：Light / System / DarkMenuOpen（play 打开菜单断言 menuitemradio 的 aria-checked）/ SwitchToDark（play 选择深色后断言 html 主题类 `dark` 换入 `light` 换出）；每 Story 独立 `createPinia` + 显式 `setPreference` 隔离状态；Portal 挂 body 的元素用 `within(document.body)` 查询。README 结构段 layout 条目补 ThemeSwitcher。
- 涉及：`apps/web/src/components/layout/ThemeSwitcher.vue`（新增）、`ThemeSwitcher.stories.ts`（新增）、`AppShell.vue`、`apps/web/.storybook/main.ts`、`apps/web/package.json`、`pnpm-lock.yaml`（lucide-vue-next 1.0.0）、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `test:run`（16 用例）/ `build` / `storybook:build` 通过（SB 静态产物验证后清理；`vue-tsc --noEmit` 漏报 stories 未使用变量而 build 报出，与此前备注一致，验证以 build 为准）；vite dev 浏览器核对：头部太阳图标按钮、菜单弹层配方与勾选指示渲染正确，选「深色」后 html=dark、页面换深色表面、菜单关闭、触发器换月亮图标，重开菜单勾选指示正确落在「深色」。

## 2026-09-06 16:40:28 +08:00 — ui 组件改为独立文件夹 + 统一导出，引入 Storybook 10 并补 Button Story

- 完成：按用户约定调整 `components/ui/` 结构——每个组件独立文件夹（`ui/button/Button.vue`，git mv 保留历史），新增 `ui/index.ts` 作为跨 Feature 稳定 Barrel 出口（`export { default as Button }`，前端应用架构规范 §5.2 的例外场景）。引入 Storybook 10.6.0 + `@storybook/vue3-vite`（与《前端技术栈》10.x 基线一致，官方支持 Vite ^8）：`.storybook/main.ts` 用 `viteFinal` 显式挂 `@tailwindcss/vite`（Storybook 独立 Vite 实例不继承 vite.config.ts，不挂则 main.css 编译不出 utilities）、`preview.ts` 引入应用唯一样式入口 `src/styles/main.css` 保证 Token 一致；package.json 增 `storybook` / `storybook:build` 脚本，`storybook-static` 入 .gitignore，tsconfig.node.json 纳入 `.storybook/**/*.ts` 并补 `vite/client` 类型（CSS 副作用导入）。新增 `button/Button.stories.ts`（CSF3，按《前端测试规范》§11.3 每个 Story 一个状态）：Default（play 内点击断言 onClick 透传，spy 先 mockClear 保证可重复）、Variants（7 变体对照）、Disabled、Pending、LongContent、DarkTheme（`.dark` 包裹块令子树按深色语义值解析，不依赖主题切换插件）。踩坑记录：Button 未声明 emits、click 走 attrs 透传，`Meta.args` 类型不含原生 `onClick`，故用模块级 `fn()` 在模板 `@click` 承接而非塞进 args。
- 涉及：`apps/web/src/components/ui/`（Button.vue 移入 `button/`、新增 `index.ts` 与 `button/Button.stories.ts`）、`apps/web/.storybook/{main,preview}.ts`（新增）、`apps/web/package.json`、`apps/web/tsconfig.node.json`、`apps/web/.gitignore`、`apps/web/README.md`（命令表补 Storybook、结构段更新组件组织方式、质量设施条目摘除 Storybook）、`pnpm-lock.yaml`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `build` / `storybook:build` 通过（静态产物验证后清理）；Storybook dev 浏览器抽查：侧栏 6 个 Story 状态点全绿，Default 渲染带 Token 样式的胶囊主按钮且 play 两步执行无错误（点击后按钮获得焦点、Interactions 计数 2），Controls 面板正确推断 variant / type / disabled / pending 类型与 JSDoc 描述，DarkTheme 包裹块内 primary / secondary / outline / ghost / danger-soft / pending / disabled 全部按深色语义渲染。

## 2026-09-06 16:24:29 +08:00 — 实现 components/ui/Button.vue 基础按钮

- 完成：按《Vue 组件设计系统方案》§8.1 落地首个 `components/ui/` 基础组件 `apps/web/src/components/ui/Button.vue`（README 待办「UI 基础组件」）。7 个变体配方（primary / secondary / tertiary / outline / ghost / danger / danger-soft）以完整类名静态映射、hover / pressed 逐项对应 §8.1 变体表并用 `enabled:` 前缀隔离禁用态；骨架为 min-h-11 胶囊（rounded-control）+ `transition-[background-color,scale]` + `motion-safe:enabled:active:scale-97` + `motion-reduce:transition-none`；焦点环按 §8.1 提示先 `outline-hidden` 再在 `focus-visible:` 下恢复 `outline-solid outline-2 outline-offset-2 outline-ring`；pending 态用原生 `disabled`（`disabled || pending`）阻止键盘重复激活、标记 `aria-busy`，保留可读文案并内联 16px spinner（`motion-safe:animate-spin`，reduced motion 下退化为静态图标）；`type` 默认 button 防表单隐式提交；单根元素 attrs 自动透传（class 合并、事件等），`variant` 默认 primary。取舍说明：§8.1 要求 pending「保留按钮宽度与可读文案」，spinner 内联会使按钮约增宽 24px——选择了保留文案与不折叠按钮的常见做法（Ant Design / shadcn 同款），未用隐藏文案的 overlay 方案（可访问性规范 §4.3 禁止让用户误以为操作消失）。
- 涉及：`apps/web/src/components/ui/Button.vue`（新增）、`apps/web/README.md`（删除已完成待办条目、结构段补 `components/ui/`）、`docs/agent-log/2026-09.md`
- 验证：临时 demo 路由与页面浏览器核对（验证后已删除）：浅 / 深主题 7 变体、disabled 50% 透明度、pending（`aria-busy="true"`、`disabled` 属性存在、`isEnabled()=false`、spinner 渲染、文案保留）、Tab 键盘焦点环（`:focus-visible` 匹配、2px 蓝色 outline）均正确，primary hover 经 150ms 过渡稳定在 `--primary-hover`（rgb(0,104,216)）；`pnpm -F @lexi-loop/web typecheck` / `build` / `test:run`（16 用例）通过。

## 2026-09-06 15:54:23 +08:00 — 评估 apps/web MVP 完成度并在 README 登记待办

- 完成：对照 PRD §11、frontend/review-flow.md、api/words.md、api/reviews.md 与 specs/frontend/ 全部规范评估 web MVP 完成度。结论：工程骨架、设计系统主题层、`packages/api-client`（Orval 生成 + mutator，已通过真实后端冒烟）与后端 8 个端点就绪，但 `src/pages/` 四个路由页面均为占位卡片，MVP 业务未落地。据此在 `apps/web/README.md` 待办按依赖顺序登记三组条目：① MVP 业务落地（Button 基础组件 → `features/words` / `features/review` 的 Feature 层 → 导入 / 生词库 / 详情 / 复习 / 结果五类页面 → document.title 与 skip link 等页面基础设施 → 纯逻辑单测），条目内注明关键实现决策（导入不去重只聚合 count、搜索分页写入 URL、active session 恢复用 GET 逐词结果按 word join 本地快照、无 abandon 端点故「放弃本轮」只清本地快照等）；② 依赖服务端契约扩展——列表 / 详情 DTO 未返回掌握程度与复习优先级、导入接口未返回逐词结果（review-flow §3 逐词反馈无数据来源），前端不自行复算公式，契约扩展前这两处不展示；③ 保留既有质量设施与 favicon 条目。本次只做评估与待办登记，未改动业务代码。
- 涉及：`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：待办引用的文档小节（review-flow §2–§10、PRD §11、前端应用架构规范 §6.2/§8.1、设计系统方案 §8.1、交互与可访问性规范 §6.1、D004/D007/D009/D010/D011）逐一与权威文档核对一致；重新核对生成 DTO（`WordListItem` / `WordDetail` / `ImportWordsResponse`）与 `packages/api-client/src/index.ts` 导出的 8 个端点，确认列表 / 详情确无掌握度 / 优先级字段、导入确无逐词结果字段，契约缺口结论成立。

## 2026-09-06 15:16:31 +08:00 — 搭建 apps/web 应用骨架（设计系统主题层 + app 装配 + 路由页面 + 主题偏好）

- 完成：按《前端应用架构规范》§3/§4.1、《前端技术栈》§3.2 与《Vue 组件设计系统方案》落地 web 骨架，业务实现未动、页面均为占位。三个递进提交：① 设计系统主题层——`src/styles/theme.css` 承载 Radix Colors 色阶导入（gray / blue / green / amber / red 浅深双套，浅色文件先于深色）、浅深双主题 Semantic Tokens（品牌蓝校准值 `#0072e2` 系、`color-mix` 状态文字与 Danger 加深）、`@theme inline` 语义映射与角色圆角 / 系统中文字体栈，`src/styles/main.css` 为唯一样式入口；vite 接入 `@tailwindcss/vite` 与 `@/` → `src` 别名（tsconfig `paths` 同步）。② 应用装配——依赖按基线大版本引入（vue-router 4.6.4 / pinia 3.0.4 / @tanstack/vue-query 5.102.8 / reka-ui 2.10.4；vue-router 5 与 pinia 4 虽已发布但不入规范基线，显式锁回 4 / 3 大版本），`src/app/` 承载 `App.vue`、显式路由表 `router.ts`（review-flow.md §1 四页 + `/` 暂重定向 `/review` + 404 兜底，页面按路由懒加载）、`providers.ts` 一次性安装 pinia / router / VueQueryPlugin（Query 全局保守默认 staleTime 30s / gcTime 30min / retry 1 / 关闭聚焦刷新）；页面统一放 `pages/` 并以 Page 结尾、暂为占位卡片；`components/layout/AppShell.vue` 提供顶部导航（导航项 min-h-11、active-class 高亮）与主内容容器；删除占位根组件 `src/App.vue`。③ 主题偏好——`lib/storage/local-storage.ts` 薄适配（吞掉隐私模式 / 配额异常，存储不可用只失去持久化）；`stores/theme.ts` 为首个跨页面客户端 Pinia store：偏好持久化为 `{version:1,preference}` JSON（缺失 / 非法一律回退 system），解析后的主题类互斥挂 html（color-scheme 由 CSS 作用域同步），system 经 matchMedia 跟随系统变化、显式偏好不被覆盖；AppShell 头部加主题切换 select，`main.ts` 在 pinia 安装后、挂载前 `init()`，`index.html` 预置与 CSS 缺省一致的 `class="light"`。`pnpm-workspace.yaml` 的 `allowBuilds` 补 `vue-demi`（@tanstack/vue-query 依赖的构建脚本）。
- 测试：`lib/storage/local-storage.test.ts` 3 用例（委托底层 / 存储不可用不抛 / 底层抛异常吞掉）；`stores/theme.test.ts` 11 用例（无记录回退 system / 恢复持久化显式偏好不被系统覆盖 / 四类非法存储回退 / setPreference 持久化并应用互斥主题类 / system 跟随系统变化 / 显式偏好不受系统变化影响），node 环境以 `vi.stubGlobal` 桩 document / matchMedia / localStorage，不引入 jsdom。
- 涉及：`apps/web/`（package.json、vite.config.ts、tsconfig.app.json、index.html、src/main.ts、src/styles/{main.css,theme.css}、src/app/{App.vue,router.ts,pinia.ts,query-client.ts,providers.ts}、src/pages/{words/WordsPage.vue,import/ImportPage.vue,review/ReviewPage.vue,review/ReviewResultPage.vue,error/NotFoundPage.vue}、src/components/layout/AppShell.vue、src/lib/storage/{local-storage.ts,local-storage.test.ts}、src/stores/{theme.ts,theme.test.ts}；删除 src/App.vue）、`pnpm-workspace.yaml`、`pnpm-lock.yaml`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck` / `test:run`（16 用例）/ `build` 通过（产物可见路由级分包，验证后清理 dist）；浏览器冒烟（vite dev）：`/` 重定向 `/review`，四个页面与 404 兜底渲染正常、导航 active 态正确；系统深色偏好下 html=dark、画布 rgb(11,11,13)=#0b0b0d，切浅色后画布 #f5f5f5 并写入 `{"version":1,"preference":"light"}`，两主题的表面 / 阴影 / 圆角符合设计系统方案 §13 首批检查项。备注：`vue-tsc --noEmit` 漏报未使用变量而 `vue-tsc -b`（build）报出，与此前记录一致，验证以 build 为准。

## 2026-09-06 14:39:12 +08:00 — api-client 改为 Orval 从 OpenAPI 自动生成

- 完成：按《前端技术栈》§7.2 与 `overview.md` §5.2「通过 OpenAPI 文档自动生成」，把 `packages/api-client` 从手写实现改为 Orval 8.28.1（fetch client + 自定义 mutator）生成。`scripts/unwrap-envelope.mjs` 从 `docs/openapi/openapi.json` 派生 Orval 输入 `spec/openapi.json`：每个操作 200 响应剥掉 `EnvelopeXxx` 外壳直接引用 `data` 的 schema（运行时 `{code, message, data}` 解包由 mutator 统一负责，否则生成类型与 mutator 返回值不一致），仅保留 200 响应并移除 `Error*` 组件（错误模型由手写 `errors.ts` 承载）；`orval.config.ts` 以 `includeHttpResponseReturnType: false` 生成裸 data 返回类型（符合规范 §5.2「code=OK → 返回 data」）、`mode: single` + schemas 分离、mutator 指向 `src/mutator.ts`。手写侧收窄为三件：`transport.ts`（fetch 薄封装，不变）、`errors.ts`（ApiError 模型，不变）、`mutator.ts`（新——`configureApiClient` 模块级配置 + `customFetch`：Base URL 前缀、Bearer 注入、envelope 校验解包、ApiError 转换；生成代码以 `Parameters<typeof customFetch>[1]` 透传 `RequestInit`，Content-Type 与 query 序列化由生成代码负责）；删除手写 `client.ts` / `types.ts` / `words.ts` / `reviews.ts`。生成物 `src/generated/`（client.ts + model/ 18 个类型文件，operationId 驼峰化即端点函数名，DTO 类型含 3.1 type-array 的 `string | null` 与枚举 const 对象）与派生 spec 一并入库，重新生成可重建；`index.ts` 改为选择性重导出（8 个端点函数 + `export type *` model + mutator/errors）。包新增 `generate` 脚本与 README；pnpm 11 在 `pnpm-workspace.yaml` 用 `allowBuilds: esbuild: true` 批准 orval 依赖 esbuild 的构建脚本（根 package.json 的 `pnpm.onlyBuiltDependencies` 在 pnpm 11 不生效）。`apps/web`：`src/lib/api.ts` 改为 `configureApiClient({ baseUrl: env.apiBaseUrl })` 装配（`main.ts` 引入该模块触发装配），仅重导出 `ApiError` 等错误工具；Feature 层后续直接从 `@lexi-loop/api-client` 导入端点函数与类型。文档同步：`docs/architecture/overview.md` 与 `docs/README.md` 目录树注释去掉「后续」（已实际生成）、`apps/web/README.md` API Client 段落改写为 Orval 流程、新增 `packages/api-client/README.md`。
- 测试：`mutator.test.ts` 15 用例（OK 解包 / 204 / 非 JSON 与顶层结构不合法转 protocol / 非 OK 转 ApiError 含 code 与 requestId / fieldErrors 聚合 / Retry-After / network / 配置 timeoutMs 后真实超时 / 调用方 abort 与预中止不发包 / baseUrl 注入 / Accept + credentials omit / 默认无 Authorization / getAccessToken 异步注入 Bearer）；`endpoints.test.ts` 9 用例对生成函数断言 method / path / query / body / Content-Type。
- 涉及：`packages/api-client/`（orval.config.ts、scripts/unwrap-envelope.mjs、spec/openapi.json、src/generated/** 生成物、src/mutator.ts、src/{index,mutator.test,endpoints.test}.ts；删除 src/{client,types,words,reviews,client.test}.ts；package.json、tsconfig.json、README.md）、`apps/web/src/lib/api.ts`、`apps/web/src/main.ts`、`pnpm-workspace.yaml`、`pnpm-lock.yaml`、`docs/architecture/overview.md`、`docs/README.md`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/api-client generate` 重复执行稳定；包 `typecheck` / `test:run` 24 用例通过；web `typecheck`（vue-tsc 把生成物纳入程序检查）/ `test:run`（4 用例）/ `build` 通过（验证后清理 dist）；真实后端端到端冒烟（临时 PG + serve，冒烟文件验证后删除）：导入幂等累计 → 列表分页搜索 → 详情 → 自定义释义设置与清除（meaningSource 迁移）→ 开始复习 30 截断为 3 → 逐词提交 → completed 汇总 → 软删除后列表过滤 → 404 ApiError 映射（code 与 requestId），全部通过；冒烟产物已清理。

## 2026-09-06 14:14:58 +08:00 — API Client 迁移至 packages/api-client workspace 共享包

- 完成：按 `docs/architecture/overview.md` §5.1/§5.2 的 monorepo 目录约定（上一条记录误放了应用内，经指正迁移），把 API Client 从 `apps/web/src/lib/api/` 迁到新 workspace 包 `packages/api-client`（`@lexi-loop/api-client`，private，exports 指向 TS 源码 `./src/index.ts`，无构建产物，由消费端 bundler/tsc 直接处理）。包内职责：`transport.ts` / `client.ts` / `errors.ts` / `types.ts` 不变随迁；`client.ts` 去掉对应用 `lib/env.ts` 的依赖，`baseUrl` 改为 `ApiClientConfig` 注入（包不读取 `import.meta.env`）；`words.ts` / `reviews.ts` 的端点函数由模块级函数改为 `createWordsApi(client)` / `createReviewsApi(client)` 工厂，返回 `WordsApi` / `ReviewsApi` 接口；新增 `index.ts` 公共出口（`export type *` 导出 DTO）。`apps/web` 侧删除 `src/lib/api/` 目录，新增 `src/lib/api.ts` 组合出口——`createApiClient({ baseUrl: env.apiBaseUrl })` + 创建 `wordsApi` / `reviewsApi` 并重导出 `ApiError` / `isApiError` / `isAbortError`，Feature/页面从 `@/lib/api` 取用；`package.json` 增加 `workspace:*` 依赖（`pnpm add` 会尝试从 registry 拉取 workspace 包名导致失败，改为手写 `workspace:*` 后 install 链接）。包自带 `tsconfig.json`（strict / verbatimModuleSyntax / noUncheckedIndexedAccess / DOM lib）与 `typecheck` / `test` / `test:run` 脚本，devDeps typescript ~6.0.2 + vitest ^4.1.11；根 `package.json` 的 `typecheck` 由代理 web 改为 `pnpm -r typecheck` 并新增 `test:run` 聚合脚本。文档同步（避免 client 位置出现两套说法）：《前端 API 与认证集成规范》§3 目录树与模块职责表（transport / client / errors 归属包，新增应用侧组合行）、§13 生成代码位置；《前端应用架构规范》§3 目录树、§4.7 lib 职责、§8.1 分层与说明；《前端技术栈》§7.1 唯一薄封装位置、§7.2 Orval 生成物输出到包、§14 速查清单两行。web README「API Client」段落改写为包 + 应用组合的结构。
- 测试：client 18 用例与 endpoints 9 用例随迁至包内（endpoints 改用工厂创建的 `words` / `reviews` 实例，用例体不变）；env 4 用例留在 `apps/web/src/lib/env.test.ts`。
- 涉及：`packages/api-client/`（package.json、tsconfig.json、src/{index,client,transport,errors,types,words,reviews,client.test,endpoints.test}.ts，均新增/迁入）、`apps/web/src/lib/api.ts`（新增）、`apps/web/src/lib/api/`（删除）、`apps/web/package.json`、`package.json`、`pnpm-lock.yaml`、`apps/web/README.md`、`docs/specs/frontend/前端 API 与认证集成规范.md`、`docs/specs/frontend/前端应用架构规范.md`、`docs/specs/frontend/前端技术栈.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/api-client typecheck` 通过、`test:run` 27 用例全绿；`pnpm -F @lexi-loop/web typecheck`（vue-tsc 经 workspace 符号链接把包的 TS 源码纳入程序检查）、`test:run`（4 用例）、`build` 通过（验证后清理 dist）；`pnpm -r list` 确认三包识别正常、`apps/web/node_modules/@lexi-loop/api-client` 符号链接存在。

## 2026-09-06 13:56:41 +08:00 — apps/web 落地基于 fetch 的 lib/api 统一 API Client

- 完成：按《前端应用架构规范》§8.1 与《前端 API 与认证集成规范》§3/§5 建立 `src/lib/api/` 唯一 HTTP 出口（原生 fetch，无 Axios）。`errors.ts`：`ApiError` 类按 kind 六分类（http / network / timeout / abort / protocol / storage），携带 httpStatus / code / data / fieldErrors（线格式 `[{field, reason}]` 聚合为按字段映射）/ requestId / retryAfterSeconds / cause，附 `isApiError` / `isAbortError`；`transport.ts`：fetch 薄封装——`AbortSignal.any` 组合调用方信号与 `AbortSignal.timeout`，超时（TimeoutError）与调用方取消（AbortError）区分归类，固定 `credentials: 'omit'`，解析 `X-Request-Id` / `Retry-After` 与响应体，非 JSON 转 protocol 错误；`client.ts`：`createApiClient` 工厂 + 默认 `api` 实例——URL 由 `lib/env.ts` 校验后的 `VITE_API_BASE_URL`（缺省同空串走 Vite 代理）拼接，query 过滤 undefined / 空串，默认 `Accept: application/json`、有 body 才发 `Content-Type`、可选 `Idempotency-Key`，`auth` 三模式（none / optional / required）与 `getAccessToken` 窄接口注入点，envelope 顶层结构校验后按 `code === 'OK'` 解包，204 返回 undefined，认证 401 重放（规范 §10）留待 Session Manager 落地后在 client 内实现；MVP 服务端无认证端点，默认 `auth: 'none'` 并注释标注切换时机。`types.ts` 与 `docs/openapi/` 契约同步的 DTO（含 `MeaningSource` 增加零值 `''`——真实后端验证发现极简词条三层皆空时服务端返回空串，类型不能只写三枚举）；`words.ts` / `reviews.ts` 覆盖全部 8 个 `/api/v1` 端点函数（路径参数 `encodeURIComponent`）。新增 `src/lib/env.ts`（环境变量唯一入口，校验协议 / query / hash）与 `src/vite-env.d.ts`（`ImportMetaEnv` 类型补充）；`vite.config.ts` 增加本地开发 `/api` 代理（默认目标 `127.0.0.1:8080`）；引入 Vitest 4.1（规范基线 4.1+，先误装 5.0 已降级）并补 `test` / `test:run` 脚本。
- 测试：`client.test.ts` 18 用例（OK 解包 / 204 / 非 JSON 与顶层结构不合法转 protocol / 非 OK 转 ApiError 含 code 与 requestId / fieldErrors 聚合 / Retry-After / network 与 stub fetch 分类 / timeoutMs 10 真实超时归 timeout / 调用方 abort 与预中止不发包 / Accept + Content-Type + credentials omit + Idempotency-Key / query 过滤 / 默认无 Authorization / required 无 token 不发包抛 BASE.AUTH.TOKEN_EXPIRED / required 与 optional 附加 Bearer，token 支持异步）；`endpoints.test.ts` 9 用例（8 端点的 method / path / query / body 与路径参数编码）；`env.test.ts` 4 用例（缺省空串 / origin+路径去尾斜杠 / 非 http(s) 与带 query 报错，经 vi.stubEnv + resetModules 动态加载）。
- 涉及：`apps/web/src/lib/api/`（errors.ts、transport.ts、client.ts、types.ts、words.ts、reviews.ts 及 client / endpoints 两个测试）、`apps/web/src/lib/env.ts`、`apps/web/src/lib/env.test.ts`、`apps/web/src/vite-env.d.ts`、`apps/web/vite.config.ts`、`apps/web/package.json`、`pnpm-lock.yaml`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：`pnpm -F @lexi-loop/web typecheck`、`test:run`（31 用例）、`build` 全部通过（验证后清理 dist）；另以真实后端端到端冒烟（临时 PG 容器 + migrate up + serve，冒烟文件验证后删除）：导入（encounters/created/updated）→ 列表分页搜索 → 详情 → 自定义释义设置与清除（meaningSource 迁移）→ 开始复习 30 截断为 3 → 逐词提交 → session completed 汇总，以及 404 错误映射（code=BASE.NOT_FOUND.USER 且 requestId 捕获），全部符合契约；冒烟产物（临时测试 / 二进制 / 容器）已清理。备注：`vue-tsc --noEmit` 曾漏报 `ListWordsParams`（interface 无隐式索引签名）赋 `ApiQuery` 的错误、`vue-tsc -b` 才报出，参数类型改为 type alias 解决。

## 2026-09-06 13:32:52 +08:00 — apps/web README 增补脚手架清理待办清单

- 完成：在 `apps/web/README.md` 新增「待办」小节，登记清理脚手架时留下的 4 项后续工作：`src/styles/main.css` 全局样式入口待设计系统落地（规范 §4.8）、favicon 待品牌设计后补 `public/` 与 `index.html` 引用、规范依赖（Vue Router / Pinia / TanStack Query / Tailwind v4 / Reka UI + Radix Colors / ESLint / Vitest）待按需引入并落地 §3 目录结构、占位根组件待 §4.1 `app/` 装配后替换；注明完成后删除条目。
- 涉及：`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：待办引用的规范小节号（§3 / §4.1 / §4.8）与《前端应用架构规范》实际标题核对一致；README 顶部已有的 docs 与 specs 目录链接未改动。

## 2026-09-06 13:26:05 +08:00 — 清理 apps/web 脚手架演示内容

- 完成：删除 create-vite 模板演示资产——`src/components/HelloWorld.vue`（含空目录）、`src/assets/`（hero.png / vite.svg / vue.svg，含空目录）、`public/icons.svg` 与 `public/favicon.svg`（模板品牌图，favicon 待品牌设计后引入）、`src/style.css`（296 行模板演示样式及其 main.ts 导入，全局样式入口 `src/styles/main.css` 待设计系统落地时按《前端应用架构规范》§335 建立）。`App.vue` 换为最小占位根组件（注释指向后续装配规范），`index.html` 改 `lang="zh-CN"`、标题 `LexiLoop`，`README.md` 换为 `@lexi-loop/web` 包说明（职责 + 常用命令 + 文档链接）。工程配置（package.json / vite.config.ts / tsconfig / .vscode）不动。
- 涉及：删除 `apps/web/src/components/HelloWorld.vue`、`apps/web/src/assets/{hero.png,vite.svg,vue.svg}`、`apps/web/public/{favicon.svg,icons.svg}`、`apps/web/src/style.css`；修改 `apps/web/src/App.vue`、`apps/web/src/main.ts`、`apps/web/index.html`、`apps/web/README.md`、`docs/agent-log/2026-09.md`
- 验证：grep 确认 `apps/web` 内无已删文件的残留引用；`pnpm -F @lexi-loop/web typecheck` 与 `pnpm -F @lexi-loop/web build` 通过（验证后清理 dist）。

## 2026-09-06 13:20:07 +08:00 — .gitignore 排除 .zcode 目录

- 完成：根 `.gitignore` 新增「ZCode」分组，忽略 `.zcode/` 目录，避免本地 ZCode 工作区文件入库。
- 涉及：`.gitignore`
- 验证：`git check-ignore` 确认 `.zcode/` 下文件命中 `.gitignore:22` 规则；`git status` 中临时创建的 `.zcode/` 未出现（已清理），仅 `.gitignore` 本身为待提交变更。

## 2026-09-06 13:20:00 +08:00 — 根目录初始化 pnpm workspace 并纳入 apps/web

- 完成：按《前端技术栈》§4 落地 monorepo 根配置——`package.json`（private 根包，`packageManager: pnpm@11.13.1` 精确锁定 pnpm 版本，dev / build / typecheck 经 `pnpm -F` 代理到 web）、`pnpm-workspace.yaml`（成员 `apps/*` 与 `packages/*`，目录树与 docs/README.md 预留布局一致）、`.node-version`（声明 Node 24 LTS 基线）、根 `.gitignore`（node_modules / dist / coverage / *.tsbuildinfo / .env / 日志 / OS 文件）。`apps/web` 的 Vite 脚手架首次入库并纳入 workspace：包名 `web` 改为 `@lexi-loop/web`、补 `typecheck: vue-tsc --noEmit` 脚本，删除脚手架自带的独立 `pnpm-lock.yaml` 与 `node_modules`，改由根目录统一安装——根 `pnpm-lock.yaml` 成为唯一 lockfile。未预装规范中的按需依赖（TanStack Query、Tailwind、ESLint、Vitest 等），留待各自工作单元引入。
- 涉及：`package.json`、`pnpm-workspace.yaml`、`.node-version`、`.gitignore`、`pnpm-lock.yaml`（均新增）；`apps/web/**`（脚手架首次提交，`package.json` 更名 + typecheck 脚本）、`docs/agent-log/2026-09.md`
- 验证：`pnpm install` 识别 2 个 workspace 项目并复用 store 完成安装；`pnpm -F @lexi-loop/web build`（vue-tsc -b + vite build）通过、产物正常；根 `pnpm typecheck` 代理脚本可达 `vue-tsc --noEmit` 且通过；`pnpm -r list` 确认根与 `@lexi-loop/web` 均为 private 包；`git status` 确认 node_modules / dist 已被忽略。

## 2026-09-06 12:54:00 +08:00 — 接入 huma v2 并离线生成 OpenAPI 3.1 spec

- 完成：以 huma v2（v2.39.1）+ humagin 适配器接管 `/api/v1` 的参数绑定、校验与响应渲染，落地 README 下一步清单中的全部定制点。`internal/handler/v1` 重写为 huma 声明式操作（路径自带 `/api/v1` 前缀，DTO 以 huma schema 标签替代 gin binding 标签）；`internal/handler/httpresp` 拥有 `Envelope[T]` / `NoData` 成功外壳与覆盖 `huma.NewError` 的错误模型（校验失败 422 降为 400 + `BASE.PARAM.VALIDATION_FAILED` + `data.fieldErrors [{field, reason}]`，RateLimited 经 huma `HeadersError` 透传 `Retry-After`，不实现 ContentTypeFilter 使 Content-Type 保持 `application/json`，未预期错误不透传 cause）；`handler/openapi.go` 离线构建 OpenAPI 3.1 spec（不启动 HTTP、不连库，`components.schemas.Error` 由错误模型类型派生，移除 huma 自动追加的校验 422、仅保留开始复习的业务前置 422 并写明 `BASE.BIZ.USER_DISABLED` 语义）；新增 `lexi-loop openapi` 命令（组合根 `app.BuildOpenAPISpec` 入口），产物 `docs/openapi/openapi.json` / `openapi.yaml` 由生成器维护。未挂载 `/openapi` / `/docs` 路由、未引入向响应体注入 `$schema` 的 SchemaLinkTransformer。status 段落同步：`apps/server/README.md`（下一步清单清空）、`apps/server/AGENTS.md`、`docs/backend/structure.md`（§2 目录树、§3 CLI、§4.4）、`docs/README.md`（目录树与快速定位登记 docs/openapi/）。
- 涉及：`apps/server/go.mod`、`apps/server/go.sum`、`apps/server/internal/handler/{router.go,openapi.go,openapi_test.go}`、`apps/server/internal/handler/httpresp/response.go`、`apps/server/internal/handler/httpresp/response_test.go`、`apps/server/internal/handler/v1/{word.go,review.go}`、`apps/server/internal/handler/v1/dto/{word.go,review.go}`、`apps/server/internal/handler/middleware/recovery_test.go`、`apps/server/internal/handler/router_integration_test.go`、`apps/server/internal/app/openapi.go`、`apps/server/cmd/{openapi.go,root.go}`、`docs/openapi/{openapi.json,openapi.yaml}`、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/backend/structure.md`、`docs/README.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...` 通过；`go test ./...` 全部通过（handler 组件测试经 httptest 驱动完整 HTTP 栈验证端点契约不变，含 400 校验 / 404 / 422 业务前置 / 空值语义 / Content-Type 断言；httpresp 单测覆盖 kind→状态映射、422 降 400、fieldErrors 位置映射、Retry-After；OpenAPI spec 单测验证 8 个操作、Error/ErrorData/FieldError 组件形态与 Envelope 响应引用）；`go run . openapi` 实际生成产物且 JSON 可解析。

## 2026-09-06 11:57:00 +08:00 — README 下一步清单写入 huma 接入待办及契约适配点

- 完成：`apps/server/README.md`「下一步（按依赖顺序）」由「当前无待办」替换为 huma 接入待办：以 gin 适配器接入 huma v2 替代 handler/v1 手写绑定校验、离线生成 OpenAPI 3.1 spec 至 `docs/openapi/`（不挂载路由）。定制点对照已核实（huma main 分支 error.go / huma.go 源码）：校验失败默认 422 降为 400（`BASE.PARAM.VALIDATION_FAILED`，项目 422 已被 FailedPrecondition 占用）、错误体保持 `{code, message, data}` + `data.fieldErrors [{field, reason}]`（huma 默认 RFC 9457 `application/problem+json`，无业务 code）、Content-Type 保持 `application/json`、RateLimited 透传 Retry-After、成功响应以泛型 `Envelope[T]` 包装 output 保住外壳与空值语义、同步改写 `components.schemas.Error` 定义使 spec 与实际一致、生成方式为离线命令并登记 `docs/README.md` 目录树。此为纯待办登记，未引入 huma 依赖、未改任何代码。
- 涉及：`apps/server/README.md`、`docs/agent-log/2026-09.md`
- 验证：待办内容与《Go 技术栈.md》（huma 为 API 文档选型、spec 放 `docs/openapi/` 不挂载）及《HTTP API 设计规范.md》（§1/§3/§4/§5、§11 状态映射）一致；README 内相对链接指向存在的文件。

## 2026-09-06 11:32:00 +08:00 — app.Run 的 stdlib log 切换为注入的 zerolog logger

- 完成：`app.Run` 签名增加注入的 `zerolog.Logger` 参数——`cmd/root.go` 在根命令 `PersistentPreRun` 中捕获 `logger.Init` 返回值到包级 `appLogger`，`serve` 命令将其传入 `app.Run(ctx, cfg, log)`。`server.go` 三处 stdlib log 改为 zerolog 结构化输出：DB 关闭失败 `Error().Err(err).Msg("close database")`、监听开始 `Info()` 带 `addr` 字段、优雅关闭完成 `Info()`；`newEngine` 弃用全局 `zerolog/log.Logger`，把注入 logger 传给 `handler.RegisterRoutes`（对齐 structure.md §4.4「组合根将日志传给 RegisterRoutes」，消除全局 logger 的过渡用法）。README「下一步」清单第 1 项完成、清单清空。
- 测试：无新增测试（`internal/app` / `cmd` 为组合根与 CLI 路由层，沿现状不含测试文件；本次仅替换日志来源，既有测试覆盖不变、全部通过）。
- 涉及：`apps/server/internal/app/server.go`、`apps/server/cmd/root.go`、`apps/server/cmd/serve.go`、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 十五个包全部通过（testcontainers 集成含容器真实运行，无回归）；另以真实 PG 容器端到端冒烟：migrate up → serve → `/healthz` 200，监听行输出 `INF lexi-loop http server listening addr=:18080`、请求日志带 request_id 等字段，均已是 ConsoleWriter 格式（原 stdlib 格式不再出现）。备注：本会话的 Bash 工具环境会拦截 SIGTERM / SIGINT（最小 `signal.NotifyContext` 复现程序同样以 143 退出且处理器未触发），优雅关闭路径的代码自骨架阶段未变、无法在工具内复验；`stopped` 行与 listening 行经同一 `log.Info()` 通道，无独立风险。

## 2026-09-06 11:20:00 +08:00 — 实现 handler/middleware 自定义中间件并替换 gin 内置

- 完成：`internal/handler/middleware` 四个占位落地，`router.go` 弃用 gin.Logger / gin.Recovery。`request_id.go`：X-Request-Id 优先透传（去首尾空白、超 128 截断），否则生成 8 位短 UUID；最先挂载使 panic / 提前中断的响应也携带 id，写入响应头与 gin.Context，`FromContext` 供链路日志取值。`logger.go`：zerolog 请求日志——method / path（含 query）/ status / bytes / client_ip / latency 单行字段，状态 ≥500 记 ERROR、≥400 记 WARN、其余 INFO，并携带 request_id。`recovery.go`：panic 统一经 `httpresp.AbortError` 按内部错误渲染（500 + ERROR，message 不含 panic 值与堆栈），panic 值 / 堆栈 / request_id 只进服务端日志；响应已提交时不再改写、仅中断链路。`cors.go`：封装官方 gin-contrib/cors（新增直接依赖 v1.7.8），允许方法 / 请求头 / 暴露响应头按《前端 API 与认证集成规范》§11.2 后端 CORS 要求设置，不开 Allow-Credentials（前端 Bearer + credentials: 'omit'）；显式 Origin 白名单，含 "*" 在构造期 panic 拒绝（gin-contrib/cors 会把 "*" 当作允许任意 Origin），空白名单不输出任何 CORS 头（等价仅同源）。挂载顺序 request_id → recovery → cors → logger（预检与非法 Origin 请求不进请求日志）。`router.go` 改为 `RegisterRoutes(r, Options{Log, CORSAllowedOrigins}, wordH, reviewH)`，组合根传入全局 zerolog logger 与白名单；`infra/config` 新增 `LEXI_HTTP_CORS_ALLOWED_ORIGINS`（逗号分隔，`parseCSVList` 解析），缺省本地开发源 localhost:5173 / 127.0.0.1:5173；structure.md §4.4 组合根表述同步。
- 测试：middleware 包纯单元测试（httptest + gin.TestMode，JSON 日志行解析断言字段与分级）：request_id 生成 / 透传 / 空白与超长处理 / 响应头与上下文一致 / 未挂载时 FromContext 为空；logger 记录字段完整与三级分级；recovery 未写出时统一 500 且响应体不泄露 panic 值与堆栈、已提交响应不再改写；cors 预检 204 与 Allow-Methods / Allow-Headers / Max-Age / Vary: Origin、实际请求 ACAO 与 Expose-Headers、白名单外 403 无放行头、无 Origin 不受影响、空白名单不输出 CORS 头、"*" 构造期 panic。config 包补 CORS 白名单默认值 / 环境变量覆盖（逗号 + 空白项）/ `parseCSVList` 用例。
- 涉及：`apps/server/internal/handler/middleware/`（request_id.go、logger.go、recovery.go、cors.go 及 4 个测试）、`apps/server/internal/handler/router.go`、`apps/server/internal/handler/main_test.go`、`apps/server/internal/app/server.go`、`apps/server/internal/infra/config/`（config.go、config_test.go）、`apps/server/go.mod`、`go.sum`、`docs/backend/structure.md`（§4.4）、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（middleware / config 新用例全绿，其余含 testcontainers 集成无回归）；真实 PG 容器 + `serve` 冒烟：无 Origin 请求响应带 `X-Request-Id`（8 位短 id），白名单 Origin 请求返回 ACAO + `Vary: Origin`，预检 OPTIONS 返回 204（Allow-Methods、Max-Age 43200），白名单外 Origin 403，上游 X-Request-Id 原样透传，请求日志按状态分级（200 → INF、400 → WRN）且带 request_id 字段。

## 2026-09-06 10:39:57 +08:00 — 实现 internal/infra/logger 日志初始化并接入 CLI

- 完成：新增 `internal/infra/logger`（structure.md §4.5 infra 技术组件）。`New(Options)` 以 zerolog `ConsoleWriter` 构造人类可读的 Logger（时间 / 级别 / 消息 / 字段列，时间列格式 `time.DateTime`，JSON 事件内部仍为 zerolog 默认 RFC3339）：`Options.Level` 为最低输出级别（零值即 zerolog 的 DebugLevel，不另行改写；推荐取值以包级常量 `DefaultLevel = InfoLevel` 显式传入）、`Options.Out` 缺省 `os.Stderr`、`Options.NoColor` 强制关色。zerolog 的 ConsoleWriter 自身不做终端检测（只看 `NoColor` 字段），故包内以 `go-isatty`（随之转为直接依赖）自行判断：仅当 Out 为终端时着色，重定向到文件 / 管道不产生 ANSI 转义序列。`Init(Options)` 在 `New` 基础上接管全局默认日志——替换 `zerolog/log.Logger` 并 `zerolog.SetGlobalLevel`，返回构造的 Logger 供未来请求日志中间件显式传递。`cmd/root.go` 在根命令 `PersistentPreRun` 中以 `Level: logger.DefaultLevel` 初始化，serve / migrate / import-ecdict 三个子命令统一生效；CLI 参数化级别（如 `--verbose` / `LEXI_LOG_LEVEL`）留待需要时接入 config。
- 测试：logger 包纯单元测试——ConsoleWriter 输出格式（含级别 / 消息 / 字段 / 时间列正则，非 JSON）、Options.Level 过滤、非终端目标（缓冲 / 普通文件）无 ANSI 色码、Init 接管全局（全局日志改写、全局级别生效、返回 Logger 可用，t.Cleanup 还原全局状态）、isTerminal 非终端分支。
- 涉及：`apps/server/internal/infra/logger/`（logger.go、logger_test.go）、`apps/server/cmd/root.go`、`apps/server/go.mod`、`go.sum`、`docs/backend/structure.md`（infra 树补 logger）、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（logger 4 用例，其余包无回归）；真实 PG 容器 CLI 冒烟：migrate up 输出 `… INF database migrations applied successfully direction=up`（重复执行幂等），dirty 状态触发 `… ERR failed to run migrations …` 且非零退出——migrations 包经全局 log 的输出已统一为控制台格式。

## 2026-09-06 10:25:00 +08:00 — 实现 internal/importer/ecdict ECDICT 离线导入与 import-ecdict 命令

- 完成：`internal/importer/ecdict` 从占位落地。`parser.go`：`RowReader` 流式解析 ECDICT CSV（encoding/csv，`FieldsPerRecord=-1` 容忍列差；表头按列名映射，大小写 / BOM / 常见别名容忍、同义列先出现者优先、未知列忽略，缺少 `word` 列报错），字段映射——`word` → headword（空词行跳过并计数）、`phonetic`（含 ukphone / usphone 别名）→ phonetic_uk / phonetic_us、`translation` 解析为结构化 raw_meanings（按行拆分，行首「词性.」前缀归一英文全称与 data-model §4 示例 / 未来在线来源对齐，半角 / 全角分号拆义项，同词性连续行合并，无前缀行整体作义项）、`exchange` 解析词形变化表并以原形键 "0" 作 lemma（缺失由 `domain.NewDictionaryEntry` 回退 headword，与运行时 `resolveLemma` 同一约定）、`bnc` / `frq` / `collins` / `oxford` 归入 frequency（键 `bnc_frequency` / `coca_frequency` / `collins` / `oxford`，0 与非数字视为无数据不写入，data-model §8 已同步补充后两者）、`tag` 空格拆分为 tags、source 固定 "ecdict"（CSV 无版本信息，source_version 留空）；`definition`（英文释义属 V2 Enrich 链路，enrichment.md §4）/ `pos`（词性占比统计而非义项词性）/ `detail` / `audio` 不导入。`importer.go`：`Importer` 按固定批次（默认 1000 行，`--batch-size` 可调）分事务提交，每批经 `repo.UpsertBatch`（ON CONFLICT headword DO UPDATE，COALESCE 保留已有 review_meanings）写入；批次失败整批回滚并中止导入，已提交批次保持有效，重跑同一文件幂等续传（structure.md §5.3）；同批重复 headword 就地覆盖（保留后出现行）避免「ON CONFLICT 同一行作用两次」的语句级错误；死锁 / 序列化失败（`repo.ErrConflict`）对整批事务做 3 次上限带抖动指数退避重放——独立于 `service.runReplayableTx` 实现，依赖方向不允许 importer 引用 service（structure.md §1）；ctx 取消在批次边界停止；进度经 `onProgress` 逐批回调。`cmd/import_ecdict.go`：Cobra 入口只做参数绑定与生命周期——`LEXI_DATABASE_URL` 经 `database.New` 建连、CSV 路径 `ExactArgs(1)`、SIGINT / SIGTERM 经 signal.NotifyContext 在批次边界停止、进度单行刷新 stderr + 结束统计 stdout；`notImplemented` 占位辅助随之移除（最后一个占位命令已实现）。
- 测试：parser 单元测试（表头映射 6 用例含 BOM / 别名 / 缺列报错、mapRow 完整行 / 最小行 / 空词跳过 / 词形词条、translation / exchange / frequency / tags 表驱动；固定非 UTC now 验证 UTC 时间戳）；ecdict 包 testcontainers 集成测试（包级 `TestMain` 共享 postgres:18-alpine + 全部迁移，与 repo / service 同模式）：小型固定 CSV 字段映射与统计（满批 + 收尾批次各提交一次）、幂等重跑（无重复行、词典字段刷新、source 接管、review_meanings 不被清空）、批次回滚续传（测试库临时触发器注入 failword，失败批整体回滚、已提交批保留，移除注入后重跑补齐全部行）、同批重复词覆盖；Go 测试规范 §2「离线导入用小型固定 CSV + 真实 PostgreSQL 验证幂等重跑、批次回滚和字段映射」全部落地。另以真实 PG 容器 CLI 端到端冒烟：migrate up → 两次 import-ecdict（幂等刷新、无重复行）→ 缺连接串 / 文件不存在均非零退出。
- 涉及：`apps/server/internal/importer/ecdict/`（parser.go、parser_test.go、importer.go、main_test.go、importer_integration_test.go）、`apps/server/cmd/import_ecdict.go`、`apps/server/internal/domain/dictionary.go`（Frequency 注释同步 frequency 键）、`docs/dictionary/data-model.md`（§8 补充 collins / oxford 保留）、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 十个测试包全部通过（importer/ecdict 单元 + 集成真实跑容器，其余无回归）；`go test -short` 集成用例正确跳过。无新增第三方依赖。

## 2026-09-06 09:35:00 +08:00 — 实现 internal/service 业务用例与 internal/handler/v1 版本化 Handler

- 完成：`internal/service` 落地全部业务用例（具体类型、请求 / 结果类型无 json 标签）。`sampler.go`：具体 `WeightedSampler`，Efraimidis–Spirakis（A-Res）加权随机不放回抽样，随机源构造注入支持确定性测试，互斥锁保护并发 StartSession 复用；`NewTimeSeededSource` 供组合根。`dictionary.go`：MVP 本地 Lookup——纯归一 → headword 命中 → 借 exchange 表键 "0" 解析 lemma（原形词条缺失或指向自身时按「不确定不归并」保留原词）→ 未命中创建仅含 headword 的最小词条；非导出 `lookup(ctx, bun.IDB, word)` 接收调用方事务，ImportWords 复用同一实现不嵌套事务（structure.md §4.3）。`word.go`：ImportWords（内存归一 / 校验 / 聚合重复与同词条词形 → 单事务内逐词解析 → 一次 ON CONFLICT 批量累计并恢复软删除，created / updated / encounters 统计）、ListWords（分页 + search，批量取词条组装展示读模型）、GetWord（软删除视同不存在）、UpdateReviewMeaning（nil 清除自定义，D007）、DeleteWord（软删除 D004）；预期错误统一映射 apperr（word not found 等）。`review.go`：StartSession 严格按 structure.md §5.1 单事务完成 advisory lock → 锁定并 Abandon 旧 active → 同一 now 计算权重 → 加权抽样 → 建 session 与全部 pending items（D009 截断；候选为 0 不建 session 返回 422）；SubmitResult 按 §5.2 固定锁顺序 session → item → user_word，Domain `Submit` 与条件 UPDATE 并存，不存在 / 错配 / 已作答按 API 契约幂等短路，pending 清零时 Complete 回写汇总；GetSession 汇总从 review_items 实时统计保证与逐词结果一致。`tx.go`：`runReplayableTx` 对唯一约束冲突 / 死锁 / 序列化失败做有上限（3 次）带抖动指数退避的**整事务**重试，超限映射 Conflict / BASE.BIZ.CONCURRENT_UPDATE；`internalError` 兜底包装。`internal/handler/v1`（dto + Word / Review Handler）落地：DTO 拥有 JSON 契约（camelCase、空列表 `[]`、无数据 `{}`、completedAt / lastReviewedAt / 清除语义用 null），WordDetail 含 definition（MVP 恒空串，V2 Enrich 填充）与 meaningSource；Handler 只做绑定、UUID 路径参数解析、page/pageSize 默认与上限（1 / 20 / 100）、DTO 转换与 Service 委托，响应一律经 httpresp。`handler/router.go` 挂载全部八个 `/api/v1` 端点；`internal/app/server.go` 组合根显式组装 `*bun.DB` → 具体 Service → 版本化 Handler（`serve` 自此必须提供 `LEXI_DATABASE_URL`），全局中间件暂保留 gin 内置实现。
- 测试：sampler 单元测试（固定种子可复现、不变量、分布统计断言）；service 包 testcontainers 集成测试（共享 postgres:18-alpine 容器 + 迁移）：Lookup 各路径、导入聚合 / 并发同词竞态 / 软删除恢复、列表分页搜索过滤、三层释义取值，以及 structure.md §5.4 全部必测场景——并发开始两轮仅一个 active、同 item 并发提交只累计一次、最后两 item 并发提交 session 必 completed、item 更新后注入失败整体回滚（直接 SQL 篡改 total_count 触发 Complete 不变量失败）、items 中途失败不留半成品 session、SQLSTATE 40001 触发重试上限（测试库临时触发器注入，t.Cleanup 移除）；handler 包 httptest 组件测试驱动完整 HTTP 栈：响应外壳与 400 / 404 / 422 契约体、分页钳制、meaningSource 迁移、导入统计、复习全流程（截断 / 放弃旧轮 / 幂等短路 / 自动完成汇总）。用例名中文短语、testify、共享数据库不并行。
- 涉及：`apps/server/internal/service/`（word.go、dictionary.go、review.go、sampler.go、tx.go 及 4 个测试文件）、`apps/server/internal/handler/v1/`（word.go、review.go、dto/word.go、dto/review.go）、`apps/server/internal/handler/`（router.go、main_test.go、router_integration_test.go）、`apps/server/internal/app/server.go`、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/backend/structure.md`（service 树补 tx.go）、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 九个包全部通过（service / handler 新集成测试真实跑容器，其余无回归）；另以真实 PG 容器端到端冒烟：migrate up → serve → 导入（encounters/created/updated）→ 列表分页 → 开始一轮（30 截断为 2）契约均符合文档。

## 2026-09-06 08:30:00 +08:00 — 实现 internal/apperr 类型化应用错误与 handler/httpresp 统一响应

- 完成：`internal/apperr` 落地为类型化应用错误：`Kind`（InvalidArgument / Unauthenticated / PermissionDenied / NotFound / Conflict / FailedPrecondition / RateLimited / InternalKind，附 `String` 供日志）与集中维护的稳定业务 code（HTTP API 设计规范 §4.1 表格全量：OK、ERROR、BASE.PARAM.VALIDATION_FAILED、BASE.AUTH.TOKEN_EXPIRED、BASE.AUTH.FORBIDDEN、BASE.NOT_FOUND.USER、BASE.BIZ.CONCURRENT_UPDATE、BASE.BIZ.RATE_LIMITED、BASE.BIZ.USER_DISABLED；MVP 契约中生词 / session 未找到复用 `BASE.NOT_FOUND.USER`、无可复习生词复用 `BASE.BIZ.USER_DISABLED`，均已在常量注释标注出处）；`Error` 结构（Kind / Code / Message / RetryAfter / Data / Err）与 `New` / `Internal` 构造函数，`Unwrap` 保留 cause 供 `errors.Is` / `errors.As` 穿透，`Error()` 文本含 cause 仅用于日志排查，对外一律走 Message 字段。`internal/handler/httpresp` 落地为统一响应：`Response{code,message,data}`；`Success` 输出 200 + 固定 `success`，nil data 渲染为 `{}`（列表 `[]` / 分页结构由 DTO 负责）；`Error` / `AbortError` 经 `errors.As` 识别 apperr 并按 kind 映射 HTTP 状态——唯一的 kind→状态映射只存在于本包；非应用错误、未识别 kind 与 nil 统一按 500 的 ERROR 处理且不向客户端暴露 cause；`RetryAfter > 0` 时输出整秒（向上取整）Retry-After；`Error.Data`（如 fieldErrors）透传进 data。
- 测试：`internal/apperr` 单元测试（kind String 表、冻结 code 字面量与接口文档一致、New 字段保留与 cause 穿透、`errors.As` 经 `fmt.Errorf` 包装后仍可还原）；`internal/handler/httpresp` httptest 单元测试（全 kind→状态码表、Retry-After 向上取整、cause 不出现在响应体、nil data→`{}`、Success 数据透传、`Error.Data` 透传、AbortError 中断链）。表驱动 + testify + 中文子测试名，独立用例 `t.Parallel`。
- 涉及：`apps/server/internal/apperr/`（error.go、error_test.go）、`apps/server/internal/handler/httpresp/`（response.go、response_test.go）、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（apperr / httpresp / domain 单元测试全绿，infra/database、internal/repo、migrations 集成测试无回归）。无新增第三方依赖。

## 2026-09-06 02:18:00 +08:00 — 实现 internal/repo 数据访问适配器与真实迁移 DDL

- 完成：`migrations/` 占位 SQL 落地为四表真实 DDL——`000001` 创建 `dictionary_entries`（headword 唯一索引、lemma 索引、释义 / 词形 / 词频 JSONB 列的 jsonb_typeof CHECK）；`000002` 创建 `user_words`（dictionary_entry_id 外键 + 唯一、`review_count = remember_count + forget_count` CHECK、软删除与自定义释义列）、`review_sessions`（status / counts / totals CHECK）、`review_items`（双外键、`(session_id, position)` 与 `(session_id, user_word_id)` 唯一、result CHECK），以及 D010「全库至多一个 active session」的部分唯一索引；down 迁移按依赖逆序 DROP。`internal/repo` 从占位落地：`internal/schema`（四实体 bun 映射 + `jsonb.go` 通用 JSONB 包装，Valuer 写 string 由 PG cast 为 jsonb）；`errors.go`（ErrNotFound / ErrAlreadyExists / ErrReferenceViolation / ErrConflict，按 SQLSTATE 归一化 + LIKE 转义）；`DictionaryRepo`（FindByHeadword / FindByID / FindByIDs / FindByLemma / Insert / UpsertBatch，重跑导入刷新词典字段但 COALESCE 保留已有 review_meanings）；`UserWordRepo`（FindByID / LockByID / List 分页搜索（JOIN 词典表匹配各层释义）/ ListActive / UpsertEncounters（ON CONFLICT 累计 + 恢复软删除 + `RETURNING (xmax = 0)` 区分 created/updated）/ UpdateCustomReviewMeaning / SoftDelete / UpdateReviewCounters）；`ReviewRepo`（advisory xact lock（FNV 派生固定键）、LockActiveSession / LockSessionByID / LockItem（FOR UPDATE）、InsertSession / UpdateSession / InsertItems、UpdateItemIfPending（api/reviews.md §3 条件 UPDATE RETURNING user_word_id）、CountResults、ListSessionItems（JOIN 出 headword））。
- 测试：`internal/repo` 新增单元测试（normalizeError SQLSTATE 映射、escapeLike、requireAffected）与 testcontainers 集成测试（包级 `TestMain` 共享 postgres:18-alpine 容器并先应用全部迁移：四实体 schema ↔ domain 往返、唯一 / 外键 / 部分唯一索引约束归一化、遇词 upsert 三态（新建 / 累计 / 恢复软删除）、自定义释义设置清除、列表分页 / created_at 倒序 / 四层释义搜索 / 软删除过滤、session 与 items 往返、条件提交幂等与归属短路、Complete / Abandon 回写、advisory lock 串行化（第二事务超时阻塞、释放后可获取）、FK 违规）。bun 侧踩坑已验证：嵌入结构体表名继承需 `bun:",extend"`（逗号前缀）、`InsertTableAlias` 使 DO UPDATE 内须用别名引用目标行、`RawQuery.Scan` 无 dest 会走 scan 路径（advisory lock 用 Exec）。
- 涉及：`apps/server/migrations/`（4 个 SQL）、`apps/server/internal/repo/`（dictionary.go、user_word.go、review.go、errors.go、main_test.go、repo_test.go、repo_integration_test.go、internal/schema/ 四个文件）、`docs/backend/structure.md`（schema 目录树补 jsonb.go）、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（repo 集成测试真实跑容器，migrations / infra/database 无回归，migrations 集成测试同时验证真实 DDL 的 up/down 可逆）；`go test -short` 集成用例正确跳过、单元测试仍运行。无新增第三方依赖。

## 2026-09-06 01:16:00 +08:00 — 将包级 TestMain 抽取到各自的 main_test.go

- 完成：`internal/infra/database` 与 `migrations` 两个包的包级 `TestMain`（连同共享容器常量、包级共享状态与 `requireTestDB` / `requireTestConnStr` 访问辅助）从各自的 `*_integration_test.go` 移入新增的 `main_test.go`；集成测试文件只保留用例与用例专属辅助（`migrationVersion` / `expectedVersion`）。测试代码与行为不变，共享容器仍由 `TestMain` 启动一次、`-short` / 无 Docker 跳过语义不变。
- 涉及：`apps/server/migrations/main_test.go`（新增）、`apps/server/migrations/migrations_integration_test.go`、`apps/server/internal/infra/database/main_test.go`（新增）、`apps/server/internal/infra/database/database_integration_test.go`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（database、migrations 集成测试真实跑容器）；`go test -short` 集成用例正确跳过。

## 2026-09-06 01:02:00 +08:00 — 实现 internal/domain 领域模型与纯业务规则

- 完成：按 docs/dictionary/data-model.md、docs/review/data-model.md 与 docs/backend/structure.md §4.1 落地 `internal/domain`。`dictionary.go`：DictionaryEntry、Meaning 值对象（JSON 形态即释义 JSONB 结构，data-model §4）、`EffectiveReviewMeaning` 三层取值纯函数（custom ?? review ?? raw，D007）与 `NewDictionaryEntry` 工厂（lemma 为空时回退 headword，normalization §3）；`user_word.go`：UserWord、`NewUserWord` 工厂（初始遇词次数必须为正）、`ApplyReview`（review / remember / forget 累计 + api/reviews.md §4 streak 规则）、`ReviewWeight` / `MasteryScore`（algorithm.md §3–§6 公式，固定参数用具名常量对齐文档，weight 不落库 D011）；`review.go`：ReviewStatus / ReviewResult 类型化常量、ReviewSession（`New` / `Complete` / `Abandon`）与 ReviewItem（`New` / `Submit`）状态机（pending → 已作答仅一次；Complete 校验 remembered + forgotten = total_count）；`normalization.go`：`NormalizeWord`（trim + lowercase，lemma 解析与「不确定不归并」留给 DictionaryService）；`errors.go`：8 个领域 sentinel 错误。工厂统一生成 UUID v7、以 UTC 记录时间戳，now 由调用方显式传入；domain 无 bun / 基础设施依赖。`google/uuid` 随之转为直接依赖。
- 测试：4 个测试文件共 13 个测试函数（表驱动 + testify，用例名中文场景短语，独立用例 `t.Parallel`；固定时间以非 UTC 时区构造并断言 UTC 归一与 UUID v7 版本位）。覆盖：归一规则、词条工厂与三层取值（含空切片视为未设置）、生词工厂与 ApplyReview（含失败不改累计、streak 六种迁移）、ReviewWeight（新词 6.0、algorithm §5 示例 ≈8.83、streak 降权、overdue 封顶与一周加 1）、MasteryScore（50% / 66.7% / 33.3% / 83.3%）、session 数量校验与 Complete 汇总一致性（含负数）、Abandon 只允许 active、item 创建与 Submit 幂等短路、状态常量字符串值。
- 涉及：`apps/server/internal/domain/`（dictionary.go、user_word.go、review.go、normalization.go、errors.go、4 个 `*_test.go`）、`apps/server/go.mod`、`go.sum`、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（domain 全绿，infra/database 与 migrations 集成测试无回归）；`go test -short` 正常跳过集成用例、domain 单元测试仍运行。

## 2026-09-06 00:42:00 +08:00 — 实现 migrations 迁移执行入口并接入 migrate 命令

- 完成：新增 `migrations` 包（`apps/server/migrations/migrations.go`，按结构决策放在 migrations 目录而非 infra/database）：embed 内嵌本目录 `*.sql` + golang-migrate v4（postgres 驱动 + iofs 来源）+ zerolog。`Migrate(url, direction, steps...)`：`up` 应用全部未执行迁移；`down` 回退指定步数（缺省 1），步数先按当前版本遍历迁移源截断为实际可用数（与 D009 同一截断语义）——golang-migrate 的 `Steps` 在最低版本返回 `os.ErrNotExist`、超额返回 `ErrShortLimit`（且发生在已回退之后），均不适合直接暴露给 CLI，故执行前截断并如实记录实际回退步数。`cmd/migrate.go` 接入（`migrate up` / `migrate down [steps]`；缺失 `LEXI_DATABASE_URL`、非法步数有友好报错），`notImplemented` 辅助移至 `import_ecdict.go`，删除 `internal/infra/database/migrate.go` 过期占位。适配器位置变更已同步权威文档。
- 测试：`migrations_test.go`（4 个单元用例：未知方向先于建连报错、down 步数必须为正整数、未注册 scheme 在驱动查找阶段失败、不可达端口建连失败）+ `migrations_integration_test.go`（包级 `TestMain` 共享 postgres:18-alpine 容器：空库完整 up、版本 / dirty 簿记、重复 up 幂等、down 全部与超额步数截断、缺省步数、最低版本再 down 视为无变化、up↔down 可逆）。
- 涉及：`apps/server/migrations/migrations.go`、`migrations_test.go`、`migrations_integration_test.go`、`apps/server/cmd/migrate.go`、`cmd/import_ecdict.go`、删除 `internal/infra/database/migrate.go`、`apps/server/go.mod`、`go.sum`、`apps/server/README.md`、`apps/server/AGENTS.md`、`docs/backend/structure.md`、`docs/specs/backend/Go 技术栈.md`、`docs/agent-log/2026-09.md`
- 验证：`go build ./...`、`go vet ./...`、`gofmt -l` 无输出；`go test -race -count=1 ./...` 全部通过（migrations 6 用例：4 单元 + 2 集成；infra/config、infra/database 无回归）；`go test -short` 集成用例正确跳过；另用真实 PG 容器 CLI 端到端验证 up→版本 2、重复 up、down→版本 1、down 5→版本 0、底部 down、坏参数与缺 URL 均以非零码退出。

## 2026-09-05 23:59:00 +08:00 — 实现 infra/database 连接池与 config 数据库配置（含 testcontainers 测试）

- 完成：`internal/infra/database/database.go` 从 TODO 占位落地为 bun + pgx/v5/stdlib 连接池构造：`Options`（URL + MaxConns/MinConns/MaxConnLifetime/MaxConnIdleTime）、`New`（ParseConfig → 合并连接池参数 → pgxpool → stdlib → bun，Ping 验证连接，失败关闭 wrapper 与连接池）、`mergePoolOptions`（0 值保持 pgx 默认）。`internal/infra/config` 扩展 `DatabaseConfig`（`LEXI_DATABASE_URL` / `_MAX_CONNS` / `_MIN_CONNS` / `_MAX_CONN_LIFETIME` / `_MAX_CONN_IDLE_TIME`，0 值不设默认、语义为 pgx 默认）。新增依赖：bun / bun-pgdialect、pgx/v5、testify、testcontainers-go(+modules/postgres)。
- 测试：`database_test.go`（mergePoolOptions 单元：全零保默认 / 非零覆盖 / 部分覆盖、非法 URL、不可达服务返回错误）+ `database_integration_test.go`（包级 `TestMain` 启动**一次共享**的 postgres:18-alpine 容器供全部用例复用，等待策略为 `ForLog("database system is ready to accept connections").WithOccurrence(2)`——postgres 首次启动会先跑 initdb 临时实例、随后重启为正式实例，只等端口会抢在 init 前连接被重置；覆盖真实建连与查询、config 连接串与池参数端到端、带池参数建连）+ `config_test.go`（database 默认与 LEXI_ 环境变量映射）。`-short` 或无 Docker 时集成用例跳过、纯单元测试仍运行。全部测试 `go test -race` 通过；`go build ./...` 与 `go vet ./...` 通过。
- 涉及：`apps/server/internal/infra/database/database.go`、`database_test.go`、`database_integration_test.go`、`apps/server/internal/infra/config/config.go`、`config_test.go`、`apps/server/go.mod`、`go.sum`、`apps/server/README.md`、`apps/server/AGENTS.md`
- 验证：`go test -race -count=1 ./internal/infra/...` 通过（database 8 个用例：5 单元 + 3 集成，共享单容器约 4s）；`go test -short` 只跑单元测试并跳过集成用例；`go build ./...`、`go vet ./...` 通过。

## 2026-09-05 23:14:00 +08:00 — 建立 apps/server 子树 AGENTS.md

- 完成：新增 `apps/server/AGENTS.md`，作为 server 子树内 Agent 的补充指引，覆盖当前 MVP 骨架状态、权威文档速查表、常用命令、分层实现要点（含常见踩坑的文档小节指针）、MVP 边界与冻结决策、变更时的文档同步纪律；只引用不复制 schema / 公式 / 决策正文。根 `AGENTS.md` 的导航段补一句指向该子树指引。
- 涉及：`apps/server/AGENTS.md`、`AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：文档内所有相对链接路径（`../../docs/...`、`../../AGENTS.md`、`README.md`）均指向存在的文件；内容与 `docs/backend/structure.md`、README 现状、冻结决策索引一致，无复制式表述。

## 2026-09-05 23:05:00 +08:00 — 更新 AGENTS.md 简介为 monorepo 表述

- 完成：删除 AGENTS.md 开头的「代码不在本仓库」，改为按 pnpm monorepo 布局同仓保存代码与文档的简介（`docs/`、`apps/server` Go 后端、`apps/web` Vue 前端、`packages/`），目录树以 `docs/README.md` 为权威，不重复维护。全仓库已无该过时表述（agent-log 历史记录除外）。
- 涉及：`AGENTS.md`
- 验证：grep 确认除本日志外无其他「代码不在本仓库」类表述；`AGENTS.md` 内引用的文档路径均存在。

## 2026-09-05 22:41:00 +08:00 — 搭建 apps/server Go 骨架（可启动 + /healthz）

- 完成：按 monorepo 布局在 `apps/server/` 建立 Go 后端骨架，module 为 `github.com/dongwlin/lexi-loop/apps/server`。当前交付为「可启动的 Gin 服务器 + 根路径 `/healthz`」，业务逻辑未实现、各层为注释占位：`cmd`（serve/migrate/import-ecdict）、`internal/app`（组合根 + 优雅关闭）、`infra/config`（viper + `LEXI_HTTP_ADDR`，已实现）、`handler/router.go`（/api/v1 空挂载点 + 契约注释）、以及 domain/repo/service/importer/handler/middleware/apperr/infra/database 与 `migrations/`（000001_dictionary / 000002_review up/down 占位）。目录结构对齐 `docs/backend/structure.md` §2，实际把 handler 放在 `internal/handler/`。
- 涉及：`apps/server/**`（约 45 个文件，全部新增）
- 验证：`go build ./...` 与 `go vet ./...` 通过；`go run . serve` 后 `curl /healthz` 返回 200 `{"status":"ok"}`，未注册业务路由 404；SIGTERM 触发优雅关闭日志。
- 备注（文档待澄清，未改动）：`AGENTS.md` 开头「代码不在本仓库」与 monorepo 文档（`docs/README.md`、`architecture/overview.md` 规定后端在 `apps/server/`）脱节；本次代码落点按 monorepo 文档执行。业务路由前缀实测为 `/api/v1`（`docs/api/words.md`、`docs/api/reviews.md`），与通用规范示例 `/v1` 不同。

## 2026-09-05 16:00:00 +08:00 — 补全前端规范文档索引

- 完成：将 `docs/specs/frontend/` 下 7 个前端规范文件补充到 `docs/README.md` 的目录结构与权威关系表中，并同步更新 `AGENTS.md` 的文档地图、权威表与冲突裁决规则。
- 涉及：`docs/README.md`、`AGENTS.md`、`docs/agent-log/2026-09.md`
- 验证：检查目录结构树与权威表行数一致，链接可正确指向各规范文件；冲突裁决规则覆盖所有前端规范。

## 2026-09-05 15:27:39 +08:00 — 建立 Agent 工作记录机制

- 完成：建立按月分文件、月内时间逆序的 Agent Log，并要求 Agent 在修改仓库前阅读最近记录作为辅助上下文。
- 涉及：`AGENTS.md`、`docs/README.md`、`docs/agent-log/README.md`、`docs/agent-log/2026-09.md`
- 验证：检查日志入口、记录范围、时间格式、逆序规则、多 Agent 写入边界与权威文档裁决关系均已明确。
